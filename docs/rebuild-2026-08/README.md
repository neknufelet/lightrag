# 2026-08 重建：診斷與方案（**快照，不要修改內容**）

這個目錄是 2026-08-06～07 那次全面盤點的原件。**它記的是當時的狀態**，
不是現況——所以裡面的現在式陳述會逐漸變假，這是預期的，不是錯誤。

**不要為了讓它「跟上現況」而改動內文。** 改了就不再是那次盤點的紀錄，
而這批文件的價值正是「當初看到什麼、據此決定了什麼」。要記新狀態就
往下面的「快照之後發生了什麼」加一列。

## 這批東西怎麼來的

三個 workflow、共 38 隻 agent，全程唯讀：

| 產出 | 怎麼來的 |
|---|---|
| `00-合併方案.md` | 綜合 A（文件結構）與 B（營運規則）兩份裁決，並把 19 筆 blocking 折入 |
| `01-白話版.md` | 同一份方案的非技術版 |
| `02-坑清單體檢表.md` | 173 條坑的人讀版；機器可讀版在 `tests/pits.json` |
| `10-文件結構裁決.md` | 15 隻 agent：5 盤點 → 3 對照 → 3 方案 → 1 裁決 → 3 對抗 |
| `11-營運規則裁決.md` | 12 隻 agent：4 現制盤點 → 2 落差 → 3 設計 → 1 裁決 → 2 對抗 |
| `20-`／`21-` 對抗審查 | 5 席敵意審查的完整異議（19 blocking／41 major／15 minor） |
| `22-坑清單完整性批判.md` | 「什麼沒被挖到」 |

三份裁決都被對抗席判為 `flawed-but-fixable`，**沒有一份是定案**。
折入修正之後的版本才是 `00-合併方案.md`。

## 快照之後發生了什麼

只增不改，一列一件。

| 日期 | 事件 | 使前面哪些陳述變假 |
|---|---|---|
| 2026-08-07 | coder 端 checkout 從 `lightrag-v1` 改名為 `lightrag`，兩台自此同路徑，也符合 ghq 慣例 | `10-` 的 B1「工作台路徑」那一列、`11-` 的座標列、`20-` 裡多筆以「兩台不同名」為前提的異議。**異議本身的推理仍然成立**（文件不該記字面路徑），只是舉的例子已被修掉 |
| 2026-08-07 | 173 條坑落地為 `tests/pits.json` ＋ `tests/test_pits.py`（commit `7ad05a6`） | `00-` 第十二節「執行順序」的第 1 步已完成 |
| 2026-08-07 | `NEXT.md` 修掉三處：開場白的工作目錄、座標的 repo 路徑、以及「`/data/rag` 從此與本專案無關」那句錯誤陳述 | — |

## 已知：這批文件裡的 file:line 錨點會失效

文件層一旦重排，`CLAUDE.md:23` 這類錨點就指不到原本的東西了。
這是已登記的問題（`tests/pits.json` 的 PIT-125：錨點失效就是條目過期的訊號），
對策是替錨點加驗證，不是把錨點刪掉。**在那支檢查寫出來之前，
引用這裡的行號要先自己核對一次。**
