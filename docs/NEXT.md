---
title: NEXT — 現在這一輪要做什麼
date_created: 2026-06-20
date_modified: 2026-08-19
status: living
kind: sop
supersedes: ""
superseded_by: ""
summary: "只放這一輪。做不做都不影響新庫上線的在 BACKLOG.md。一條一行、動詞開頭，箭頭後面是完成的判準。"
---

# NEXT — 現在這一輪要做什麼

**這裡只放「現在」。** 不擋這一輪的搬去 [BACKLOG](BACKLOG.md)，上線之後再撈回來。
> 2026-08-19 分家：PO 打開看到 49 條以為都是接下來，實際擋著上線的只有幾條。
> 「一條 49 行的清單沒有人讀得完，讀不完的清單等於全部失效」（上游 A02 的原話）。

做完的**刪整行**。`→` 後面是「怎麼知道做完了」，寫不出來的不算待辦。理由外引：
知道但不做 → [KNOWN_ISSUES](KNOWN_ISSUES.md)；裁決 → [decisions/](decisions/)；
發生過的事 → [cairn/LOG.md](../cairn/LOG.md)。**現在的狀態不寫在這裡** —— 跑
`scripts/compat-check.py`，或看 `/data/lightrag/checks/latest.json`（每天 08:30 寫的）。

## 新庫上線（順序與判準在 [路標](roadmap-rag-acoustic-20260817.md)，18 條裁決也在那）
- ⬜ 三個查詢 skill 改網址與 workspace　→ 新庫獨立（第 6 步那支檢查會數它）
- ⬜ `mount-guard`／`backup-cold.sh` 加上新 stack　→ 新庫也被守著、也有備份
- ⬜ `lightrag-snapshot` 這個 backrest plan 還指著舊資料根　→ 新庫起來後改指
- ⬜ 走完路標第 3 步到第 9 步：複製 models → 刪舊庫 → 建新的 → 「沒沾到舊東西」檢查 → PO 挑 3 篇走一遍 → 清回全空 → 交給 PO
- ⬜ 走一次**異地**還原（restic → Google Drive）　→ 列得出快照、抽得出一個檔。⚠ 本機還原點 2026-08-19 已實走驗過（3 份 → 0 份，models 沒少），但那防的是「這批放錯檔案」，異地那份防的是「機器沒了」，**不能互相代替**

## 確認清單的規則還在砍（每砍一條，PO 在新庫就少按那麼多次）
- ⬜ 剩下 117 項還沒歸類的規律（2026-08-19 全庫重算、當作沒人確認過）：投稿日期漏網 13、ResearchGate 下載頁 6、單獨作者名約 30、作者單位 6、`ARTICLE INFO` 4、掃壞的字 4、其餘一份一份不同　→ 各自變成規則或明確裁定「就是要人看」
- ⬜ 人名那條漏掉沒有縮寫點的寫法（"Michael Stinson"）　→ 量新庫的分佈再決定要不要放寬
- ⬜ 「我先作答、你再對答案」那支　→ 前 20 份藏我的答案（誠實基準），之後才顯示

## 改規則之後沒有人告訴你誰要重抽
- ⬜ 一支檢查：拿現在的規則重算每一份計畫，跟「這份進庫時用的規則」比，列出不一致的並說「這幾份要重抽」　→ 需要先在計畫檔記下「進庫時的規則版本」，現在沒有這一格
- ⬜ 金絲雀基準還是舊庫的 319 份（全部「消失」）　→ 重設成新庫的，否則每天紅而理由是假的
- ⬜ 圖片 caption 裡的版面字（`caption="Journal Pre-proof"`）沒有規則管　→ 消音規則只碰文字區塊，碰不到 caption

## 等 PO 裁
- ⬜ 要不要加「`/opt/stacks/lightrag/` 底下不准有第二個含金鑰的檔」這條檢查　→ 2026-08-19 清掉 5 份躺了十天的舊備份，靠的是人看到，不是機器擋
- ⬜ 要不要另做一頁覆核「規則有把握直接丟」的那些　→ 現在只在每一份底下點得開
- ⬜ 換掉 2026-08-08 外洩的 `LLAMA_API_KEY`（在容器命令列上，`docker inspect` 看得到）　→ 那個容器重新啟用之前換掉
