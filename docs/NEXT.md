---
title: NEXT — 現在這一輪要做什麼
date_created: 2026-06-20
date_modified: 2026-08-21
status: living
kind: sop
supersedes: ""
superseded_by: ""
summary: "只放這一輪。順序與完成判準在 goal.md，這裡是它的一頁摘要。一條一行、動詞開頭，箭頭後面是完成的判準。"
---

# NEXT — 現在這一輪要做什麼

**這裡只放「現在」。** 不擋這一輪的搬去 [BACKLOG](BACKLOG.md)。

> 2026-08-19 分家：PO 打開看到 49 條以為都是接下來，實際擋著上線的只有幾條。
> 「一條 49 行的清單沒有人讀得完，讀不完的清單等於全部失效」。

**完整的目標、順序、與「做完長什麼樣」在 [goal.md](goal.md)。** 這一頁是它的
摘要，兩邊不一致時**以 `goal.md` 為準**。

做完的**刪整行**。`→` 後面是「怎麼知道做完了」，寫不出來的不算待辦。
**現在的狀態不寫在這裡** —— 跑 `scripts/compat-check.py`，或看
`/data/lightrag/checks/latest.json`（每天 08:30 寫的）。

## 第 1 批 · 讓體檢表說實話
- ⬜ 每天量了的碎字與漏字結果回寫體檢表（走既有的 `ledger.record()`）　→ 「未設定」從 860 格掉下來，掉的數字對得上 172×2
- ⬜ 碎字檢查 165/172 都在叫（96%）：先量分布再重訂門檻，**不要直接關掉**　→ 拿得出分布，最乾淨的那幾份不再被叫
- ⬜ `run-tests.sh` 加 `-ra`，並斷言 dker 上那幾條必須真的跑到　→ 跳過哪幾條看得見
- ⬜ 契約的「驗不了」數量進 `latest.json`，**不改離開碼**　→ 報告上數得出來

## 第 2 批 · 正常操作不會誤殺
- ⬜ 無主失敗不再殺整批（兩天兩次，觸發條件是 PO 的正常操作）　→ 依斷言編號分文件層／整庫層；⚠ 清單要跟 PO 對過；兩次事故各留一條回歸測試
- ⬜ 補「永久移除」：一個指令清完解析包／圖檔／體檢表／知識庫紀錄　→ 中途斷掉重跑一次就收乾淨，不留孤兒

## 第 3 批 · 補上沒有人在看的缺口
- ⬜ 抽取被安靜切掉（2026-08-21 重量：**4 次、4 篇**，舊版的 8 是 log 行數；`finish_reason=length` 不報錯）　→ 有一支會數它，故意造一次切斷時它會叫
- ⬜ 檢索沒有探針（`retrieval-check` 零個呼叫端）　→ 進每日流程，知識庫被清空時它會叫
- ⬜ 89/172 份沒登記來源（A-35）　→ `source-map.py check` 清單歸零，舊庫 259 筆留著
- ⬜ 25 部沒有 Zotero 認領（A-38）　→ 基準回到 0
- ⬜ 23/30 組等價類沒審（A-37）　→ `eq-label.py audit` 補完
- ⬜ 圖譜沒蓋規則版本戳（A-32）　→ 重抽完成後 `extraction-profile.py stamp`

## 第 4 批 · 收尾
- ⬜ 關掉 backrest 那兩個 rag 排程（PO 早就說要關）　→ backrest 上看不到
- ⬜ 換掉 2026-08-08 外洩的 `LLAMA_API_KEY`　→ ⚠ 那個容器**重新啟用之前**必須先換
- ⬜ 走一次**異地**還原（restic → Google Drive）　→ 列得出快照、抽得出一個檔。本機還原點已驗過，但**不能互相代替**
- ⬜ `Invalid cross-device link` 177 次：要動掛載＝要停機　→ 排最後，需要停機視窗

## 等 PO 裁
- ⬜ `ZX35VPDU` 的 2 張空表格（體檢表上那 2 格「驗不了」）　→ 系統有把握自動修，但要人看一眼
- ⬜ 「`/opt/stacks/` 底下不准有第二個含金鑰的檔」這條要不要加　→ 2026-08-19 清掉 5 份躺了十天的舊備份，靠的是人看到，不是機器擋
- ⬜ 確認清單剩 117 項未歸類的規律　→ 各自變成規則，或明確裁定「就是要人看」
