---
title: 三塊工單 — 讓「沒在守」跟「守了沒事」長得不一樣
date_created: 2026-08-21
date_modified: 2026-08-21
status: living
kind: sop
supersedes: ""
superseded_by: "docs/goal.md"
summary: "PO 2026-08-21 問「到底要怎樣才能亮燈有人守」。答案不是加檢查，是把塌成兩態的地方補回三態。A 立基準（守衛下線中）、B 沒量過要亮燈、C 兩個會反覆咬人的判準。"
---

# 三塊工單（2026-08-21）

> ## ⚠ 這份已經被 [goal.md](goal.md) 取代
>
> **A 做完了**（金絲雀 172 份基準、離開碼三態、實地演習）。
> **B 與 C 還在**，但已經連同新開出來的缺口一起重排進 `goal.md` 的四批。
>
> **這裡留著的唯一理由是「驗收機制」那一節** —— 那一節仍然有效，
> `goal.md` 的「動手前必讀」第 4 條就是指它。其餘看 `goal.md`。

PO 的原話：**「從一開始就在那邊沒人守要不就沒亮燈，你到底要怎樣才能亮燈有人守
做一些有用的事情」**。

## 為什麼是這三塊，為什麼是這個順序

**今天一整天的問題是同一個形狀：「沒在守」與「守了沒事」在報告上長得一樣。**

| 當天實例 | 表面 | 實際 |
|---|---|---|
| `test_stack_matches_repo` | 報告寫 `passed` | skipif 路徑過期，在 dker 上也跳過 |
| `canary` | 每天「金絲雀通過」 | 基準是 `{}`，守著 0 份 |
| 體檢表五個閘門 | 一整欄 `-` | 172 份全部從沒量過 |
| `compat` 的 `ok=None` | 燈號是綠 | 那幾條斷言其實驗不了 |

這個專案**本來就有三態**（通過／失敗／驗不了，鐵則 7 那一族），規則寫在文件裡。
問題是實作上一路塌回兩態——遇到不確定就回綠。

⇒ **A 排第一不是因為它最大，是因為沒有它，B 與 C 的改動都是盲改。** 金絲雀是
「改了規則有沒有改壞」的唯一防線，而它現在下線。在它立起來之前動任何判準，
沒有任何東西會告訴你動壞了。

⚠ **量與判可以並行，改判準不行。** 改動都會碰同一套規則與同一個知識庫。

---

# 驗收機制（三塊共用，不得跳過）

**這一節排在工單前面，因為它就是工單的主題。** 這三塊做的全是「燈」，而今天
每一個問題都是「燈在該紅的時候是綠的」。⇒ **驗收方式自己不夠嚴，就是在重複
今天的錯。**

## 唯一的通過條件：那盞燈**被人看見紅過**

```
沒有人看過它紅 ⇒ 沒有證據它會紅 ⇒ 不算做完
```

`canary` 每天回綠了好幾天，`test_stack_matches_repo` 每天寫 `passed` 好幾天——
兩個都「通過」，兩個都沒在守。**綠燈證明不了任何事，只有紅過的燈才證明得了。**

所以每一項改動要交三段**原文輸出**，缺一不可：

| 段 | 做什麼 | 證明什麼 |
|---|---|---|
| **1. 對抗** | 故意製造它該抓的情境 | 它**會**紅（真陽性） |
| **2. 恢復** | 把情境改回來 | 它**會**變綠（不是永遠紅） |
| **3. 控制組** | 拿一個相似但**不該**觸發的情境 | 它**不會**亂紅（假陽性） |

⚠ 第 3 段最常被省略，而省略它的代價是本專案最貴的一種：一盞天天亮的燈，
三個月後所有人都學會無視它（`parse-check` 的 WARN 佔 96%，現在就是這個狀態）。

## 對抗要打在判準上，不是打在資料上

⚠ **不准為了讓燈紅而動知識庫的文件**（技能文件「不要做的事」第 4 條，
鐵則第 8 條）。對抗情境要用**臨時副本或測試夾具**造，做完就丟。

| 想驗的燈 | ✅ 對抗這樣造 | ❌ 不准這樣造 |
|---|---|---|
| canary 抓得到規則漂移 | 改一條規則的參數，跑完改回來 | 去動 `work/parsed/` 的解析結果 |
| 無主紅燈不再殺整批 | 測試夾具餵一筆假的 compat 輸出 | 真的去刪一份庫內文件 |
| 「沒量過」會亮燈 | 臨時把某一欄的紀錄搬走，驗完搬回 | 清掉真的體檢表 |

## 每一項都要能回答「它會漏掉什麼」

⚠ **這一條是防自己的。** 一盞燈的價值不在它抓到什麼，在它**抓不到什麼是已知的**。
`extract-check` 抓「多出來的實體」不抓「少掉的」——所以抽取被截斷它永遠不會叫，
而這件事今天才有人寫下來（見「不在這三塊裡」第一項）。

每項驗收要附一行「已知抓不到的」，寫不出來代表還沒想清楚它在守什麼。

## 誰驗收

改的人交上面三段輸出，**另一個人（或另一個 agent）照著重跑一次**。
⚠ 重跑的人要**先看不到原輸出**再跑——照著答案抄不是驗收。

---

## A. 立基準 — 讓「改壞了」會被發現　✅ **2026-08-21 晚做完**

> **做完了。** 基準 172 份（commit `2948293`），dker 實跑 `金絲雀通過：172 份`。
> 空基準改成 rc=4「擋」、沒有 bundle 改成 rc=3「驗不了」（commit `d9c5373`）。
> 驗收四段的原文輸出在 `2948293` 的 commit 訊息裡。
>
> ⚠ **這一節的對抗步驟寫錯了，照著做會看到假綠燈** —— 見下方「對抗要打在
> 判準上」那格的更正。

**要回答的問題**：現在 172 份的處理結果長什麼樣？之後改規則，哪一份的哪個數字變了？

**怎麼知道做完了**：`postprocess.py canary` 的離開碼**不再是「守著 0 份的 0」**，
而且刻意改壞一條規則時它會紅。

### 改之前的現況（2026-08-21 白天實測，晚上已修）

```
$ cat tests/canary-baseline.json
{}
$ ssh florian-dker '…/postprocess.py canary'
金絲雀通過：0 份基準文件的數字都沒變（另有 172 份新文件尚未納入基準）
canary rc=0
```

### 已經知道的坑

- ⚠ **清空基準是對的，沒做後半步才是問題。** commit `4a6e533` 清空舊庫那 319 份
  幽靈是正確處置，訊息裡也寫著「灌完第一批**要記得跑 `canary --update`**」——
  **但沒有任何機制在提醒**。這就是本工單存在的理由。
- `canary --update` **拒絕在空母體上寫**（那道守衛是對的）。現在 172 份 bundle 都在，
  跑得起來。
- ⚠ **`--update` 是「認可現況為正確」，不是「重新整理」。** 172 份只能抽查，
  抽查什麼要先講清楚——至少涵蓋今天已知有問題的那幾份（`HMJ6IDEG_10`、
  `F9FBE9QK`、`ZX35VPDU`），否則等於把已知的壞當成基準。
- 立完之後**離開碼要能區分三態**：基準 0 份、沒有任何 bundle 這兩個分支現在都
  `return 0`（`postprocess.py` `cmd_canary()`），要改成非零的「驗不了」。
  不改的話，下次基準又被清空還是安靜。

### 做完之後要能說出

```
基準份數     172（或抽查後決定的子集，要寫明為什麼）
抽查了哪幾份 以及為什麼是那幾份
空基準行為   實測改成 rc=? 、check-levels 標成哪一態
已知抓不到   （見下）
```

### 驗收三段（照「驗收機制」那一節）

⚠ **2026-08-21 更正：下表第一列的對抗方式行不通。** 實測
`layout_noise.RUNNING_HEAD_MIN_REPEAT 3→2`（消音規則改鬆一格）→ canary **回 0**。
原因：規則早就套用進解析檔了（172 份都有 `_pp_original_text`），而 `plan_one()`
吃 `ctx.items`、**不還原就重算** ⇒ 書眉頁尾的 text 已清空，門檻改鬆改緊都找不到
東西可消。**消音那一族（`noise`／`title`／`cover_ad`／`margin`）與 LaTeX 那族
改了都不會紅。** 要造真的對抗情境，改一條「目標還有內容」的規則 ——
實測 `empty_table.IMG_ONLY_TEXT_MAX 200→20` 會紅（`rc=2　漂移 K7SS62X5…
review: 1 → 0`）。

| 段 | 怎麼做 | 期望 |
|---|---|---|
| 對抗 | 改一條**目標還有內容**的規則參數（例：`empty_table.IMG_ONLY_TEXT_MAX`）→ 跑 `canary` | **紅**，而且指名是哪幾份的哪個數字變了 |
| 恢復 | 參數改回來 → 再跑 | 綠 |
| 控制組 | 不動規則，把一份**新文件**丟進 `work/parsed/` → 跑 | **不紅**（新增是 info 不是 failure，`test_canary_new_document_is_info_but_drift_is_failure` 守著這件事） |

外加一段：把 `canary-baseline.json` 暫時搬走 → 跑 → 應該是**非零的「驗不了」**，
不是現在的 `rc=0`。驗完搬回來。

**已知抓不到**（2026-08-21 晚實測，不是推測）：基準 23 欄裡 **14 欄在 172 份上
是同一個值**。

    會動的 9 欄　items 172/172、pages 172/172、refs_mute 147/172、
                tables_total 114/172、title_fired 112/172、title_held 37/172、
                held 1/172、repairable 1/172、review 1/172
    不動的14欄　mute、ratio、title_mute、cover_ad_mute、margin_mute、
                latex_×5、charts_×2 全 0；cover_ad_fired 全 False、margin_fired 全 True

⇒ **消音那一族（`noise`／`title`／`cover_ad`／`margin`）與 LaTeX 那族改了不會紅**，
因為規則已經套用進解析檔、目標的 text 已清空、計數只能是 0。
`cover_ad_fired`／`margin_fired` 兩個布林仍抓得到「規則整條不開火了」。
要真的守住那幾條，基準得從**未套用**的解析結果算 —— 另一件事，不在這輪。

另外仍然抓不到：規則改了但數字剛好不變的漂移（例如把 A 規則的命中改成 B 規則
命中、總數一樣）。

---

## B. 沒量過要亮燈 — 三個塌成兩態的地方

**要回答的問題**：現在哪些燈號其實是「不知道」在冒充「沒事」？

**怎麼知道做完了**：把今天這四個實例逐一改掉，而且每個都有「改壞它會紅」的證明。

### B-1 體檢表整欄沒量過（860/1376 格）

```
$ ssh florian-dker '…/ledger.py summary --problems'
共 1376 格：通過 508　fail 6　驗不了 2　未設定 860
```

⚠ **2026-08-21 晚更新：`fail 6` 已經是 0。** 那 6 格全部查證為假訊號並用
`ledger.py set` 寫回（三個消音比例逐項看過，三個接地的裁決本來就在
`tests/verified-findings.json`）。現在是 `通過 514　fail 0　驗不了 2　未設定 860`。
**「未設定 860」一格沒動** —— 那才是 B-1 要處理的。動手前自己重量一次。

五個閘門（`parse.coverage`、`parse.checks`、`pp.equations`、`extract.format`、
`retrieval.smoke`）172 份全部未設定。總表上它只是一堆 `-`。

⚠ **更糟的是 `parse` 與 `coverage` 每天都在跑**（`daily-check.sh`），量了、結果
寫進 `checks/`，**卻從不回寫體檢表**。所以不是「沒量」，是「量了但表上看不到」。

做法：`ledger.py summary` 加欄級統計，整欄 0 量測要明講；daily-check 跑完把
parse／coverage 的結果經**既有的 `record()` 單一入口**回寫。
⚠ `parse.checks` 的 WARN 只回寫 ERROR 層——WARN 佔 96%，直接進表會讓表跟著失去
鑑別力（見 B-2）。

### B-2 parse-check 的 WARN 沒有鑑別力（實測 96%）

```
$ ssh florian-dker '…/parse-check.py'
共 172 份： OK 5 ／ WARN 165 ／ ERROR 2      rc=1
```

判準是「空文字／空表格／空公式任一非空即 WARN」，而零星空 text 塊是 MinerU 對
這批論文的**常態產物**。ERROR 那層仍然準（歷史 2/2，今天這 2 份也各自對應到
真的碎字元）。

⚠ **不要直接把 WARN 關掉。** 先量：165 份的空塊數分布長什麼樣，門檻設在哪裡
會留下幾份。技能文件的「累積兩三份同型再改判準」在這裡同樣適用。

### B-3 測試跳過看不見

`run-tests.sh` 用 `pytest -q`（無 `-ra`），報告只有 `N passed, M skipped`。
`test_stack_matches_repo` 因此靜靜跳過好幾天——它是唯一守著「跑著的 compose 與
repo 一致」的測試，修好之後**立刻抓到真漂移**（部署那份的註解還寫著更早的
`acoustics_v2`）。

做法：加 `-ra`；再加一條「在 dker 上這幾條必須被執行」的斷言。

### B-4 compat 的斷言級「驗不了」在燈號上隱形

`compat-check.py` 的 `ok=None` 刻意不進離開碼（那是對的，驗不了不該擋），但
`latest.json` 只有整支檢查層級的 `unverified`。做法：把數量放進 `unverified`
附註（如 `compat: 2 項驗不了`），**不改離開碼**。

### 做完之後要能說出

```
每一項      改前長怎樣 → 改後長怎樣（附原文輸出）
沒動什麼    尤其 WARN 門檻若沒動，要說為什麼
已知抓不到  每一項各一行
```

### 驗收三段（四個實例各一組）

| 實例 | 對抗（該紅） | 控制組（不該紅） |
|---|---|---|
| B-1 整欄沒量過 | 臨時把 `extract.grounding` 那一欄的紀錄搬走 → summary 要說「這一欄 172/172 沒量過」 | 只搬走**一份**的紀錄 → 說「171/172」，不要升級成整欄警報 |
| B-2 WARN 門檻 | 造一份真的大量空塊的假解析 → 仍要 WARN | 拿現在 165 份裡最乾淨的那幾份 → 新門檻下**不該**再 WARN |
| B-3 skip 看不見 | 把某條 dker 專屬測試的條件弄成永遠 skip → 要被那條新斷言抓到 | 在 coder 上跑 → 該 skip 的照樣 skip，**不得報成失敗** |
| B-4 compat 驗不了 | 讓某條斷言回 `ok=None` → `latest.json` 的 `unverified` 要出現它 | 離開碼**不得改變**（驗不了不擋流程，這是既有裁決） |

⚠ B-2 的控制組最重要：**這一項的全部目的就是減少假陽性**，只驗「壞的還會紅」
等於沒驗。

**已知抓不到**：B-1 只知道「有沒有紀錄」，不知道紀錄是不是新的——一份三個月前
量的結果在表上跟今天量的長得一樣。（要不要加時效由後續決定，這一輪先不做。）

---

## C. 兩個會反覆咬人的判準

**要回答的問題**：哪些判準會在 PO 的**正常操作**下製造假失敗或漏報？

**怎麼知道做完了**：兩個都有回歸測試，而且測試在修之前是紅的。

### C-1 無主 hard 失敗會殺掉無關的整批（兩天兩次）

```
2026-08-20 00:49  1 份陪葬（XVD6N97J，其實已 processed）
2026-08-21 01:22  10 份陪葬（HMJ6IDEG_03..12，其實都已 processed）
```

`hard_failing_documents()`（`scripts/intake.py:1009`）用「檔名是不是 `what` 的
子字串」分主，**分不到主就當整庫級**，然後 `verify_batch` 把整批標失敗。

⚠ **`ba87447` 只修了一個象限**（驗到一半才進來的新檔 → 名單改成量測後才算）。
第二次是另一個象限：**已被移出語料、但留有殘 bundle 的舊文件**。觸發條件是
「PO 在批次進行中移除或歸檔庫內文件」——那是正常操作。

做法：依斷言 ID 區分「文件層（A-10～A-16，`what` 都帶檔名）無主」與「真整庫層
（A-19／A-26 那種）」。前者另記庫級警報、**不動本批判定**。
⚠ 哪些算真整庫層要跟 PO 對過清單，不要自己定義。

### C-2 刪一份文件沒有「全部清掉」這條路

今天的實例：走「放回收件匣」＋「從收件匣刪除」，而重置**刻意保留解析成果**
（MinerU 要錢，這個設計是對的）。兩個動作各自正確，組合出一個沒有主人的解析包
→ A-13 無主紅燈 → 觸發 C-1 → 10 章陪葬。

做法：補一個「永久移除」動作，一次清乾淨（bundle、crops、ledger、知識庫紀錄）。
⚠ 順序有講究：先從知識庫刪、再清磁碟——反過來的話中途失敗會留下今天這種孤兒。

### 做完之後要能說出

```
C-1  回歸測試（修前紅）＋ 真整庫層的斷言清單（PO 確認過）
C-2  一個指令走完全部四處，且中途失敗不留孤兒
已知抓不到  各一行
```

### 驗收三段

**C-1**（純函式，可以完全用夾具驗，不必碰真系統）：

| 段 | 夾具餵什麼 | 期望 |
|---|---|---|
| 對抗 | 一筆 `A-13 <某份不在本批的文件>：…` 的 hard 失敗 | 本批**全部通過**，另記一筆庫級警報 |
| 控制組 | 一筆 `A-19 pipeline 目前 idle` 的 hard 失敗 | 本批**全部擋下**（真整庫層，這是它該做的） |
| 恢復 | 沒有任何 hard | 全過、無警報 |

⚠ 今天實際發生的兩次要各留一條回歸測試，用**當時的原始錯誤字串**當夾具——
改寫過的字串證明不了它抓得到真實情況。

**C-2**：

| 段 | 怎麼做 | 期望 |
|---|---|---|
| 對抗 | 拿一份測試文件走完永久移除 → 跑 `compat-check` | **0 個 hard**（不留孤兒 bundle） |
| 中斷 | 在「刪知識庫紀錄」與「清磁碟」之間人為中斷 | 重跑一次就收乾淨（冪等），**不得**留下今天那種孤兒 |
| 控制組 | 對一份**還在庫裡**的文件下永久移除 | 要擋下來或明確二次確認，不得靜靜刪掉 |

**已知抓不到**：C-1 只看斷言 ID 分類，若之後新增文件層斷言而忘了登記進清單，
它會被當成整庫層——**所以清單要跟斷言定義放在同一處**，不要兩地維護。

---

## 不在這三塊裡（但已知）

- **抽取被安靜截斷**：8 次、4 份（`HB2XKTAW`／`GX4JFGVL`／`FUHQQYEM`／`J78THHNE`）。
  沒有任何尺在看，因為接地檢查抓「多出來的」不抓「少掉的」。→ 新探針，B 做完再談。
- **檢索完全沒有探針**：`grep -rn retrieval-check scripts/ deploy/` 零個呼叫端。
  技能文件寫「retrieval.smoke 放每日檢查」——**那句是錯的**。
- **`Invalid cross-device link` 177 次**：compose 把 `inputs` 與 `__parsed__` 掛成
  兩個掛載點，搬檔必然失敗。訊息寫著「同步之後才跳過」，所以資料沒少，只是
  原始 PDF 沒被歸檔。要動掛載＝要停機，另案。
- **`scan` 的 `\hat{o}`**：真陽性，`∂` 被誤讀成戴帽子的 o，全庫 1 處。自動修正
  清單裡沒有這個寫法。→ 走 `kb-health-check` 流程裁定。
- 完整清單（11 項）見 2026-08-21 的稽核結論，摘要在 [handoff-20260821](handoff-20260821.md)。
