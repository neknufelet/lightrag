# 密鑰 —— .env 內含 OpenAI key 與 MinerU token，絕不進版控
.env
.env.*
!.env.example

# 執行期資料（實際落在 /data，此處僅為誤建時的保險）
data/
rag_storage/
inputs/

# 來源 PDF。它們是輸入資料不是 repo 內容，而且很大（C 那份 1.6 MB）。
# 只擋 docs/ 底下 —— **不能寫成 `*.pdf`**：verdicts/work/crops/ 底下有一批
# **目錄**叫 `<檔名>.pdf`（例如 `C Equivalent Networks.pdf/verified/373.html`），
# 那條會把 173 個人工裁定一起擋掉。
/docs/*.pdf

*.log
.DS_Store
__pycache__/

# Zotero 外掛的 .xpi **要進版控**：`update.json` 的 update_link 指著它，
# 那是 Zotero 檢查更新時會去抓的網址。忽略掉的話那個連結是死的。

# `zotero-sync.py apply` 的還原點。**每次 apply 都整份覆蓋**，進版控只會製造
# 240 KB 的 diff 噪音，而它要防的（寫壞了要救回來）只需要「上一次的那份」。
# ⚠ 這跟 verdicts/ 其他東西不一樣：那些是人工判定、不可再生；這份是機器產物。
/verdicts/zotero-tags-backup.json
