# Project Cairn 流水帳

本檔以逆時序記錄實質進展 —— 最新的一則放最上面、緊接在這一行下方。每則保持簡短：
只放摘要與指標，結論沉澱進 `cairn/<topic>.md`。

## 2026-08-17（一）· 紅燈分三態；而昨晚接上的兩個守衛其實一直沒在跑

**開工先重量，第一件事就打臉昨天的收尾。** `deploy-stack.py freshness` 冒出一盞
12:11 那次沒有的紅：

```
lightrag-intake.service 啟動於 08-14 13:18
它載入的 4 個檔最後在 08-16 23:35 改過（晚 58.3 小時）
```

⇒ 昨晚接上的接地檢查（`4ee38ca`）與 graph-clean（`45fa73a`）**提交了、dker 也
pull 了、就是沒重啟**。「接上了」與「在跑」是兩件事。重啟後 `freshness` 轉綠。

**而它為什麼一整天沒被看見**：`latest.json` 的 `status` 天天是 `fail`，因為
`daily-check.sh` 把「任何非零」都當成失敗 —— 其中一盞**永遠不會綠**：
`run-tests.sh` 在 dker 回 3 的意思是「這台沒有 node，那支 JS 測試根本沒跑」，
它自己的訊息就寫著「驗不了 ≠ 失敗」。逐份看過八份 `tests-*.txt` 的尾行，
從 08-13 起同一句話。**真的紅燈就埋在那片天天都在的紅裡。**

改成三態，判準抽成 `scripts/check-levels.py` 一份，daily-check 與審核台橫幅共用：

```
擋      canary / scan / units / deploy / fresh、compat rc=2、tests rc=1
提醒    compat rc=5（它自己的定義：只有 soft 不過）、parse、coverage
驗不了  tests rc=3
```

⚠ `scan` 的 3 **刻意留在「擋」**，跟 tests 的 3 不同 —— 那不是「這台驗不了」，
是「有人去建一份基準」，做得完。數字一樣不代表意思一樣。

**上一個 commit 會踩爆的洞，當場補掉**：橫幅的條件是 `cs != "ok"`，而 daily-check
從以前到現在寫的都是 `pass` —— 只是 status 從來沒 pass 過，所以這條路一次都沒
走到。不修的話審核台會天天用警示色喊「每日檢查 pass：未指明」。提醒與驗不了改用
灰色一行（消失＝靜靜丟掉，喊＝天天紅）。

dker 實跑收尾：`status: pass`、`EXIT=0`、`blocking []`、`warnings 3`、
`unverified 1`；跑著的審核台只畫一條灰線，沒有紅的。

**順帶量到兩個同型的洞（都沒動）**：`compat-check` 的 A-38 因為沒有
`ZOTERO_API_KEY` 而「這條沒跑」，卻被算成 soft **不過** —— 同一個形狀第三次，
記進 NEXT。體檢表總表現在**拒絕輸出**，因為 08-16 那個幽靈文件
（`2026 - DART …`，job 說 indexed、解析包不存在）還在表上；處置要 `--move`，
是會動檔案的決定，留給 PO。

## 2026-08-16（日）· 人工裁定合成一份，並發現「可再生」那句話是錯的

**PO 記的是「15 張手工表格散在兩邊」，逐檔量出來比那個大：**

```
repo   173 個裁定檔（C Equivalent 11、N Flow 157、G Porous 5）
dker     7 個（只有 C Equivalent）
```

N Flow 的 157 個文字修補，**跑著的系統一個都沒吃到**。

動之前先驗最危險的一件事：8/9 清空重解析過，裁定檔的鍵是 content_list 索引，
**索引位移就是靜默寫進別的項目**。173 個逐檔對現行解析包：MISSING_ITEM 0、
TYPE_MISMATCH 0，註解宣告的頁碼與 `page_idx` 逐檔對得上。

按內容分類：53 已套用、90 只差空白、3 純插入、**27 真的改了字**。抽驗每一個都是
同一個病——MinerU 把 `∂` 讀成 `\hat{\sigma}`／`\hat{\mathcal{O}}`／`\hat{\partial}`。

**而且它還活在索引裡。** 之前記的「誤讀 143 → 0」是用探針自己的規則量的，
探針看不到這幾種。實查（`strpos`，不能用 `LIKE`——它預設拿反斜線當跳脫字元，
第一次就是這樣量到假的 0）：

```
合併前  N Flow 的 chunk 裡 hat_sigma 5／hat_mathcal_O 3／hat_partial 1，全庫 2 份中招
合併後  N Flow 0／0／0（103 個 chunk），全庫 1 份中招，總份數 317 不變
```

合併＋`apply --commit`＋`reindex`（零 MinerU 呼叫，抽取快取命中）走完。
剩下那一份是 `O Analytical`，1 處：`∂p₁/∂n` 被讀成積分上下限，**結構也壞了**，
要人看 PDF 才修得了，沒有裁定檔可用。

**`verdicts/README.md` 說那 163 個 `.txt` 可再生，對其中 19 個是錯的。** 當場驗：
拿 apply 之前的原文跑現行 `latex_fix.fix_one`，19 個真的改了內容的**重現出 0 個**；
∂ 三種樣式規則跑之前之後一樣多（5→5、7→7、1→1）。
⇒ **「檔頭寫著機械套用」是檔案自己的宣稱，不是驗證。** 要判可不可再生得真的跑一次。

⚠ 順手量到另一個同型的洞：**體檢表 dker 318 份、git 只有 20 份**，而 `note` 欄
是大段人寫的判定理由。README 的反向對照指令（「兩個數字必須相同」）今天實測
496 vs 198，早就對不上了。補齊到 322，並把那條判準改成只看危險的方向
（「dker 有而 repo 沒有」的差集）——**一個永遠會叫的判準等於沒有判準**，
這就是 20/318 擺了那麼久沒人停下來的原因。

⚠ **重抽有副作用，而且是我造成的**：`reindex` 之後 `compat-check` 的 A-33 從綠變紅
——模型又生出 `Table 16`／`17`／`21` 三個位置標記節點。這正是 08-09 記過的形狀
（提示詞守不住，靠容器外的確定性清除），用既有的 `graph-clean.py` 清掉，A-33 回綠。
⇒ **任何一次重抽之後都要重跑 graph-clean**，這件事現在只寫在這裡，沒有執行者。
順帶又發現同一個洞：08-14 的四份 graph-clean 備份也沒進版控，守衛沒有在看這個目錄
（README 早就把它列為不可再生）。補進去，並加了守衛的第六格。

**兩個懸了三天的門檻同日定案**（[ADR-0007](../docs/decisions/0007-parse-gate-thresholds.md)）：
`parse.coverage` 超標記 `pass`＋note、會叫但不擋；`parse.checks` 只有 ERROR 記
`fail`。當天實跑的母體是 317 份（不是 08-13 那個 259）：
`parse-check` OK 32／WARN 283（89%）／ERROR 2、`coverage-check` 超標 15。
ERROR 從 3 掉到 2 正是被上面那次合併修掉的——**ERROR 是修得動的，所以拿它當
擋的判準合理；WARN 283 份不是**。

### 同日稍晚：三邊同步，零停機；順帶抓到一個「證據被自己刪掉」的 bug

push → dker pull → `deploy-stack install` → `docker compose up -d` 全套走完。
**四個容器一個都沒重建**，因為那次 compose 改動只是把寫死的路徑換成帶預設值的
變數（`${PARSED_DIR:-…/work/parsed}`），而 `PARSED_DIR` 沒設、`DATA_ROOT=/data/lightrag`
—— 算出來與舊值逐字相同，docker 判定設定沒變。`fresh_rc` 與 `deploy_rc` 都轉綠。

⚠ 途中被 `guard-command.py` 擋了兩次，**兩次都擋對**：把 `$?` 接在管線後面、
以及 `docker compose config`（會把 `.env` 的值全部展開印出來）。
第三張圖剛寫完「整份清單裡只有這一支是**擋**，其餘全是**叫**」，當場示範兩次。

**手動重跑每日體檢，冒出一個早上沒有的紅燈：canary 規則漂移。**
追下去是兩件事：

一、**漂移本身是我造成的，而且是想要的** —— `C Equivalent Networks.pdf` 的
`review: 1 → 0`，因為那張留待人工確認的表當天判完了。⚠ 但 `--update` 會同時
把一堆「尚未納入基準」的新文件整批吃進去，那是 NEXT 明文禁止的，所以沒動。

二、**紅燈訊息指著一個不存在的檔案。** `daily-check.sh` 的保留步驟把不同前綴的
檔名混在一起 `sort`，而 `canary-` 字母序排在其餘七種之前 —— 總數一碰到 120 的
上限，**第一個被刪的永遠是 canary，而且是同一輪剛寫好的那一份**。
實測：清理前 121、清理後正好 120，被刪的就是它。所以 `checks/` 底下
**一個 canary 檔都沒有**。⚠ 而且 `latest.json` 根本沒有 canary 欄位 ——
**紅燈是真的，證據被自己刪了，而且機器讀不到它紅過。**

### 同日深夜：新庫設計定案，兩個守衛從「躺著」變成「在守」

**PO 決定重建：只收論文與學位論文，教科書之後再談。** 設計七張圖在
[docs/rebuild-v3-design-20260816.md](../docs/rebuild-v3-design-20260816.md)，
六件事定案（範圍、拆章接進料台、全部重新解析、新庫完全獨立、舊庫驗收後整包退休、
警報只走審核台橫幅）。

**拆章工具不用新寫。** `~/data/vibevoice/` 那支已經在跑，而且**現在庫裡那 89 份
五碼前綴的檔案就是它切的** —— 三項對照：前綴五碼章×100＋節、`_partN` 子切、
檔名砍 80 字（`…diffusene` 原文是 `diffuseness`，82 砍成 80）。

**標題頁消音修了一次，但沒有一路修下去。**

```
放寬「標題被期刊小標擠開」＋錨定字串當保險絲  →  +25 份、零誤傷（canary 8 處漂移逐項覆核）
再放寬兩次（+18 份）                        →  實測 2 份的論文標題會被當成作者行消掉
```

⇒ **停手，改走「規則只做確定的，其餘進確認清單」**（PO 裁決）。每加一層保護就多一個
會安靜失效的東西。要人看的量：631 項、165 份、平均每份 6 項，而且是預先勾好只改錯的。

**接地檢查接上了。** 判定 (`grounding_entry`) 早就寫好也驗過，缺的只有生產呼叫點。
順帶把兩處各一份的 `0.10` 合成一個家（`ledger.GROUNDING_SUSPECT_RATIO`）。
⚠ **文件寫的「全庫要一個多小時」不成立，實測 48 秒** —— 那個數字擱置了它好幾個月。
實跑：311 份過、6 份不過。

**兩個守衛的真相**（都不是壞掉，是沒開）：

```
ntfy 的兩個 -crashed 單元   殘骸，沒有任何觸發者  →  刪掉
mount-guard                機制寫得對，08-09 真的觸發過一次，之後沒人打開  →  啟動
```

⚠ 而資料根**真的在外接 USB 碟上**（`/dev/sda1`，916G 剩 858G）。先前文件說是 NVMe ——
那是 `/data`，資料在 `/data/lightrag`，**兩顆不同的碟**。守衛因此從「多餘」變成「必要」。

**收件匣 731M 全是重複**（318 份逐一比雜湊，library 全有），已清。順帶解掉「318 vs 317
差 1」：是 `2026 - DART …`，**job 說 indexed，解析包不存在、知識庫 0 列** ——
簿記說謊的第一個實例，而且沒有任何檢查會發現它。

**`NEXT.md` 666 → 80 行**，`standards-check` 的 A02 hard 紅第一次熄。刪的三類：
已完成的、**隨舊庫退休而作廢的**（母體即將不存在）、以及不是待辦的（搬回 KNOWN_ISSUES）。

**今天犯的錯（誠實記著，六次同一個形狀：先斷言再驗證）**：說資料在內接碟（PO 糾正）、
說外掛沒送 item key（早就在送）、說消音規則四支（三支）、量規則消掉 0 項（拿已消音的
資料量）、可疑率用錯分母（工具自己用的是 `ok+可疑` 不是 `total`）、說「不會壞」下一句
就找到覆蓋不留痕跡的洞。

## 2026-08-15（六）· 外掛 0.3.5（取自 commit 訊息與 CHANGELOG，非本次實測）

送出的檔名帶 Zotero item key 並放最前面、外掛自己決定送哪個 PDF（跳過中文翻譯本，
看標題＋檔名＋`BabelDOC_translated` 標籤）、分不出來就不送、對帳改用 key 查。
另外把解析目錄抽成 `PARSED_DIR`（遷移時兩座庫不能共用同一份）。
設計文件 `docs/design-one-name-20260814.md` 補上截斷者的答案：**是 Zotero 自己**
在重新命名附件時砍的，110–111 字，砍尾巴。

## 2026-08-14（五）· 標籤三個人在寫，以及第一次真的從圖譜刪掉一篇

**Zotero 的狀態標籤爛掉，是因為三個寫手各寫各的，而且其中一個還活著：**

```
外掛 0.2.0    送出打 `_toRaged`，確認打 `_RAGED`（大寫）
PO 手打        `_Raged`（小寫）、`_INGESTED`
我（08-13）    `_NOT_IN_KB`
```

238 筆 `_Raged` 裡有 **70 筆掛在子筆記或附件上**，10 筆標著「不在庫」其實在庫。
收拾時踩到最大的坑：**Zotero 的 `DELETE /tags?tag=X` 不分大小寫**，刪大寫版把
163 筆的小寫版一起帶走，靠事先存的備份逐筆 PATCH 才救回來。第二個坑是我把標籤
寫成 type=1（自動標籤），那會被「刪除本館所有自動標籤」一次清光——同名的 type 0
與 type 1 在 Zotero 眼中是兩個不同的標籤。

收斂成兩個標籤、各對應一座知識庫，並寫成可重跑的 `scripts/zotero-sync.py`
（`5b97b2c`，28 條測試）。**靠人記得對帳是撐不住的**：對帳當下 276 份，同一個
下午變成 317 份。比對的三道閘各擋掉一種當天真的發生過的誤判，實測 98 筆候選
收斂到 39 筆全部正確。外掛的大小寫源頭也堵了（0.2.1，`65e8dff`）。

**LightRAG 的單篇刪除第一次實測，而且乾淨。** 一篇大語言模型的論文（DART）誤入
聲學知識庫。`intake.py` 的註解寫著那條路「沒有人實測過」，所以先量了風險再動：

```
DART 的實體 283 個，只有 13 個與別篇共用（全是 `K`、`T`、`accuracy` 那種雜訊）
DART 的關係 293 個，與別篇共用 0 個
```

建冷備份還原點（停機 74 秒）後刪除，結果**每個數字都跟預測一致**：
實體 −270、關係 −293、chunk −14、文件 −1，13 個共用實體全部還在。

⚠ **還原點不能拿來退單篇**：它是整個資料庫的冷備份，退回去會把同批的四十幾篇
一起丟掉。⚠ **kbapi 的 `/docs` 列的是「解析過的」不是「圖譜裡有的」**（它讀
`work/parsed`）——刪完圖譜之後它照樣列著 DART，而且那份 PDF 再進收件夾會命中
快取跳過解析。解析包要一併移走（移到 `records/removed/`，5.7 MB，可還原）。

## 2026-08-13（四）· 公式交叉比對的三個前提全錯，以及圖譜裡的 376 個垃圾

**公式交叉比對上線後才發現它的三個前提都不成立，而三個是被三種不同方法抓到的：**

```
來源靠檔名猜              人工逐份核    五類全錯，四類是假報方向
骨架一樣就是同一條        三個模型投票  62 組裡 11 組不是（`#=\frac{#}{#}` 那種）
相似度高就是證據          PO 判 19 題   0.99 與 0.81 都出現在「不是同一條」裡
```

⇒ **沒有一種方法抓得到全部。** 修完之後 Tier A 原本報的 4 個「發現」**全部是假的**，
真發現仍然只有 08-12 手動查到的那一條（Maa 電阻，係數 32 vs 8）——
工具的價值是**能自動把它排到第一名**，不是找到了新東西。

三份判定凍結進 `verdicts/`（來源登記、Tier A 審計、19 題標註），
三道守衛看著（A-35 來源、A-36 供應商、A-37 審計）。

**模型只在它驗過的方向可信。** 負向控制用「完全不相干的兩條」出題，模型 100% 答對；
但拿去篩「係數不一致」時**把唯一那條真發現刪掉了** —— 真正的任務是「很像、只有係數
不同」，模型會把係數不同本身當成理由判「不是」。那支工具當天寫、當天刪。

**順手抓到的四個實際故障：**

```
眼睛 A 打不到        釘的供應商在 OpenRouter 改名了（alibaba/fp8 → alibaba）。
                     A-29 是綠的 —— 它驗金鑰有效，不驗「實際那條路走得通」。補 A-36
查詢不可重現         走圖譜沒帶關鍵詞 → 同一題三次拿到三組不同段落。
                     說明寫了很久卻擋不住任何人，kbapi 改成回 400
圖譜 376 個垃圾節點  `Chapter 1`／`Eq. 4.43`／`Table 9`。已清並接進 intake 批次收尾
漏詞假警報           軟連字號（U+00AD）把 `absorp­tion` 切成兩個「詞」。
                     **解析是對的，量測工具錯了** —— 與檔頭記的 `ﬂ` 連字同一形狀第二次
```

⇒ 當天的共同形狀：**檢查綠的，不代表它檢查的是實際會走的那條路。**

## 2026-08-12（三）· Zotero 外掛、三塊工單、以及四個「訊號沒有鑑別力」

**Zotero 外掛**（`zotero-plugin/`）：選文獻右鍵送進收件匣、打 `_toRaged`，之後自己
回頭對帳換成 `_RAGED`。Zotero 8 起選單走 `MenuManager` 且標籤只能給語言檔。
裝不上猜了四輪全沒中，開偵錯輸出一行講完：`applications.zotero.update_url not provided`
—— **而那一欄就寫在我抓來比對的那份 manifest 裡，我看到了卻沒放進去**。
拿了對照組，卻只比對自己已經想到的欄位。

**三塊工單**（[worklist-20260811](../docs/worklist-20260811.md)）＋一次 12 個 agent 的
workflow（唯讀）。三個結果：

```
檢索      中英落差量到的主要是打分模型的語言偏心，不是知識庫。文件一個字沒變、
          只把問題改成中文，分數掉 82%。⇒ 沒有證據支持重抽
體檢表    259 × 8 格裡 1940 格空白。歸檔 4 張幽靈表後總表恢復輸出
符號      62 處 → 逐處查證後 61 處（見下）
```

**四次「訊號沒有鑑別力」，其中兩次是我犯的：**

1. **收件匣冒出 `2026 - <標題>.pdf` 就斷言外掛通了** —— 收件匣裡本來就有二十幾份
   長一樣的（PO 自己也這樣命名）。已收回。
2. **「中文文獻永遠搜不到」是假警報。** 那 0/10 是拿五道專門題量的，而那份文件是
   綜述。改問一道它該回答的英文題：10 段裡 5 段來自它。**統計說謊，一道真問題就推翻。**
3. **∂ 探針的「零誤報」不再成立。** `(ρ̄ f̄)/ρ̄` 是 Favre 平均的定義式，上下同形成立
   但 ρ̄ 是真的平均密度。補上「分母要有被微分量」後 62 → 61。
4. **我修那條判準時寫成「兩側都要」，當場殺掉 20 處真誤讀**（62 → 41）。
   五條測試全綠 —— 因為那五條全是「分子有被微分量」的形狀，**母體不夠**。
   `∂/∂t` 的分子是光禿禿的算子，判準本來就不對稱。

⇒ 前三個的形狀相同：**看到一個訊號就下結論，沒問「這個訊號分得開嗎」。**
第四個的形狀是**測試全綠不等於對，當案例共用同一個形狀時**。

**當天稍晚：∂ 誤讀 71 處全部落地。** 探針補上行內斜線寫法（+10 處，原本整族看不見），
逐處判讀後修進解析結果 —— 143 個算子符號、10 份文件、探針歸零、canary 通過。
原文在 `_pp_original_*`，退得回去。逐處依據在
[partial-review-20260812](../docs/partial-review-20260812.md)。

⇒ 最值得記的一步：有兩處只看 LaTeX 判不出來（`D̂/D̂t` 可能是物質導數），
我原本要叫兩隻模型看圖投票，**結果自己把那塊裁出來看一眼就結案了** ——
而且同一條式子裡真的有一個 `Dh/Dt`（直立 D、沒帽子，探針本來就不碰）。
**有原圖可看的時候先看原圖**；模型投票是給「看了也還是不確定」用的。

⇒ PO 裁決 `reindex` **等**：符號錯誤不影響檢索到哪一段，而若之後決定重抽，
先跑就白跑一輪。

順帶：`scan-partial.py` 之前一個測試都沒有，兩代失敗換來的判斷全寫在註解裡；
現在有六條。`intake.py` 寫體檢表時「計畫沒產出表格」會寫成「通過」，回填前修掉。

## 2026-08-10（一）· 進料分組上線、六隻尺量完全庫、備份的目的地原來不存在

**知識庫 70 → 257 份，每份耗時 3.8 分鐘 → 0.77 分鐘，審核台 5.1 秒 → 0.12 秒。**

**分組**：流程切成三段（解析 → 改稿 → 一起送去讀），改稿與抽取的互斥靠**單一協調
執行緒的先後順序**，不用鎖。實測 86 份一批：併行成立、零重疊、零件因「pipeline
忙碌中」失敗。設計在 [intake-batching-design](../docs/intake-batching-design.md)。

**上線後才看得見的三個 bug，全部是我造成的：**

| | 症狀 | 成因 |
|---|---|---|
| A-19 誤殺 | 一批 89 份報 84 份失敗，而資料庫 159 份全是 processed | 契約檢查逐份跑，而它斷言「pipeline 是閒的」—— 同批鄰居還在跑 |
| 一批的尾巴 | 86 份花 28 分鐘 | 逐份跑 compat-check，那 N 次問的是同一個母體 |
| 審核台 5.1 秒 | 每 3 秒重算 400 多個 PDF 的 sha256 | 內容定址是對的，錯的是每次都重算 |

**頁面尺寸判準兩次收斂，canary 兩次零漂移。** 從「整份一致」→「要裁的那幾頁一致」
→「**那張表需要裁圖嗎**」。老掃描件的尺寸本來就不可能一致 —— 那是達不到的條件。
四份被擋下的裡只有一份是真的有問題，而它的表格 MinerU 早就抽對了。

**六隻尺對 257 份跑完，整個知識庫只有兩塊 100 多字元的雜訊要清。**

```
coverage-check   13 份超標，逐份查證的 4 份**全是假訊號**
parse-check      WARN 225（沒有鑑別力）／ERROR 2（2/2 全對，真的碎字元）
extract-check    接不到原文 0，符號型 8.7%（已知現象）
```

⇒ **難看的數字要先當成量錯**，那是鐵則第 7 條「乾淨的 0 要先當成量錯」的鏡像。
最快的判準：**抽取詞數 > 對照源詞數 → 證人涵蓋不了它，比對不成立**。
判斷流程沉澱進 `kb-health-check` skill，逐份結論記進 `verified-findings`。

**備份：目的地從來不存在。** `backrest` 的 `rag-db` 倉庫沒有被 `restic init` 過，
那兩個排程每 4／6 小時跑一次、**從設定那天起一次都沒成功**，而沒有人發現 ——
因為沒有人去看過目的地。所有訊號（有排程、有容器、有設定、有在跑）都指向正常。
細節與還原程序在 [backup-design](../docs/backup-design.md)。

⚠ **還原路徑仍然沒有人走過。** 腳本自己寫著「沒驗過的還原路徑等於沒有備份」。

**同一天的第二個洞：還原點從來沒被建出來過。** 進料按下「全部開始」時建的還原點，
拿**每日備份的上傳戳記**當跳過依據 —— 而那個戳記的意思是「已經上傳到 restic」，
不是「本機有一份可以換回去的複本」，且上傳成功後本機複本會被 `rm -rf` 掉：

```
04:00  備份 → 上傳成功 → 寫戳記 → 刪本機複本
10:00  放 PDF、按全部開始 → 指紋沒變 → 跳過 ⇒ 本機還原點不存在
```

**這不是邊角案例，是每天的常態。** 實測當下戳記與指紋逐字相同、目錄不存在。
改成兩個戳記各答各的問題，跳過前還要確認目錄真的還在（收據不是複本）。

⇒ 判斷失誤的形狀：當時的推理是「上一份備份本來就已經是這一批的還原點了」——
這句單獨看是對的，**錯在沒有把「上傳成功會刪掉本機複本」接上去**。
兩個各自正確的事實中間少了一步，而少的那一步不會自己冒出來。

⇒ 也是「沒驗過就等於沒有」的第二個實例：功能寫了、測試過、部署了、
每次都回 exit 0 —— 只是從來沒有真的建出東西來。

**我今天在同一個形狀上錯了五次**：`pgrep -f` 匹配到自己那條 ssh、`is-active --quiet`
對 `activating` 回非 0、`ps -C` 沒匹配就印全部（把 PID 1 的開機時間當成
「兩天的 restic 行程」）、JSON 解析錯誤讓我看到假的「快照數 3」、以及拿
「62 分鐘還沒傳完」當調 timeout 的依據（那時它在對不存在的倉庫空轉）。
**共同點：拿到輸出就當結論，沒先問「這個輸出長得對嗎」。**

## 2026-08-09（六）· 70 份進庫；三隻眼睛判七張表，而且推翻了同日的一條觀察

第一批收工 **70 份、21,617 節點、31,839 關聯**，`compat-check` hard 0。
最後卡住的是 `C Equivalent Networks.pdf` —— 一張表擋住整份文件。

**卡住的原因是閘門做對了事**：眼睛 A 對「這格是示意圖」**捏造了一個 imgur 網址**，
`gate_table_html` 在寫入點擋下。而 `crosscheck` 說那張表 4/4 格一致 —— 因為它只比
「兩眼一不一致」，不看「多出了什麼」。**兩眼剛好都幻覺時會全綠通過**，所以內容閘門
必須掛在寫入點，這一條當天被實地驗證了一次。

**判定過程：不靠投票，靠獨立證據。** 七張表逐格判，四種來源：

| 來源 | 解決了什麼 |
|---|---|
| **PDF 文字層的字元碼** | `(I)`／`(II)` 是大寫 I（0x49），不是 `l` 也不是 `1`。三頁一致 |
| **知識庫自己**（`A Conventions.pdf`） | 「underbar ＝ 複數量，不必要時不加裝飾」⇒ `Z̲_K` 的底線是刻意的，不能丟 |
| **PO 目視** | 三張表的 `Z_K` 全部有底線；#476 的 `p_II` 與 `Z₀v_IIx` 之間沒有橫線 |
| **第三隻眼**（MiMo，新加） | #434 是唯一同時抓到底線與大寫 I 的 |

⇒ **拿知識庫查一本書的符號約定，來判定同一本書另一章的轉錄** —— 這是重建之後
第一次真的用它做事，而不只是驗它活著。

**同日稍早的一條觀察被推翻，而且是被它自己警告過的方式推翻的。**
那條記著「眼睛 A 換成 qwen3-vl-32b 之後，捏造外部網址沒有重現」，樣本欄寫著
「⚠ 樣本就是 1 張」。今天樣本擴到 10 張，**同一個行為又出現了**，而且是
2026-08-02 舊 qwen 犯過的**同一張表**。⇒ 跨模型版本重現，是 qwen 家族的預設行為。
`model-observations.json` 已改寫，推翻的那條標明是推翻，不悄悄改掉。

**沒有任何一隻眼睛穩定抓到底線**：A 中 #434／#453 漏 #466，C 中 #434／#466 漏 #453，
B 三張全漏。⇒ 這類「細小但改變語意的裝飾」三方都不可靠，必須人看最後一眼。
**「哪一隻比較準」這個問法本身是錯的** —— 兩眼設計的前提就是錯法互補。

七張裁定寫進 `verified/`，每檔第一行記依據。重跑之後 C 進庫，**70 份 0 失敗**。

## 2026-08-09（六）· 第一批 52 份進庫，過程中撞出十個 bug

空庫起步，當天進 52 份（22 + 30）。收工時 `compat-check` **hard 0／soft 0／驗不了 0
（336 項）**、`canary` 52 份相符、查詢實測兩題撈得到原文。

**十個 bug 全部是實跑撞出來的，沒有一個是讀碼推的。** 分兩類：

**會靜靜損壞資料的三個** —— 這類最貴，因為不會有錯誤訊息：

- **致謝區段吃掉整個附錄 A**（正文 3 項、公式 4 條、圖 3 張）。成因：
  `Appendix A. …` 這一行 MinerU 沒標成標題，而區段的結束條件只看標題層級。
  **攔下它的是消音比例守衛（35.1%），而那個守衛原本被當成「門檻對綜述論文太緊」**
  —— 逐份看過消音清單才發現三份裡有一份是真的圈錯。
  ⇒ **看比例不能只看數字，要看它消掉了什麼。** 修法是加硬邊界 `BODY_RESUMES`
  （附錄／補充材料），與標題層級無關。修完該份從 35.1% 掉到 22.6%，自己通過。
- **寫 bundle 不是原子的**（`write_text` 先清空再寫）。`content_list.json` 正是
  LightRAG 掃描時會讀的東西，讀到中間態不會報錯，只會是一份內容不對的索引。
  改成暫存檔 → fsync → 改名。**這跟併行無關也該做。**
- **半張表被當整張表採用**（V1／V2 截斷偵測，見同日另一則）。

**把東西擋在門外的七個** —— 不會壞資料，但會讓進料停擺：

| | 影響 |
|---|---|
| 參考文獻規則認領頁首頁尾，與版面雜訊撞號 | 22 篇裡擋掉 **8 篇（36%）** |
| 修補撞上抽取（沒等就動手） | 30 篇裡擋掉 **11 篇** |
| 計畫不報 `apply` 會擋的事（`refs`／`title` 沒進 JSON） | 「說乾淨、動手才擋」 |
| 封面頁尺寸與內頁不同 | 30 篇裡擋掉 3 篇 |
| 「等你看」看完沒有動作可以做 | 那一節只出不進 |
| `_busy()` 守衛（parse／admit／retry 三處） | 進料期間永遠重試不了 |
| 重置保留的解析成果把自己的 PDF 判成重複 | **文件重置之後再也送不回來** |

**當天學到最貴的三件事：**

1. **「規則寫好了、沒有執行者」是這個專案最常見的失效形狀，而且我自己犯了三次。**
   分工只寫在註解裡（參考文獻 × 版面雜訊撞號）、例外只加在其中一個呼叫端
   （封面頁：`DocContext` 放行、`compat-check` A-14 說不行，**文件索引完了才失敗**）、
   判準抄一份到別的檔（圖標記壓縮）。⇒ 共用一個函式不夠，**新的分支要加在那個
   函式裡面**；共用不了時用測試把抄本釘住。
2. **警報要在它該響的情境下實測。** A-34 兩個 bug 都是「正常狀態下是綠的」——
   只有真的把碟拿掉才看得見：`Path.is_file()` 不吞 PermissionError（訊息被吃掉）、
   以及**它排在「連上容器」之後根本跑不到**（碟不見時容器也連不上）。
3. **併行的瓶頸不是設定，是「誰可以在什麼時候動檔案」。** 解析開 6 條沒問題
   （打雲端 API，純等網路）；放行開 6 條實測會撞 —— `apply` 不能在 LightRAG
   掃描時跑，而併行時總有人在掃。已退回 1 條並寫下「要真的併行得先把 apply
   挪到解析剛完成那一刻」。

**收尾**：`extraction-profile.py stamp`（A-32 轉綠）、`graph-clean.py apply`
刪 35 個位置標記節點（A-33 轉綠）、`canary --update` 建立 52 份新基準
——**今天改了五條規則全程沒有基準保護**，那是下一次才開始生效的安全網。

**還沒做**：`submit_reset` 的「忙碌中」守衛還在（進料期間重置不了）；
`kbapi` 載入時就讀 `.env`，所以它自己的邏輯測不到（純函式已搬走，其餘還在）。

## 2026-08-09（六）· 整個清空重來，資料根搬上 1TB，並補了它的警報

- **PO 裁決：22 份不值得留。** 語料總量是 300 多篇，已經處理的 20 幾份佔不到一成，
  而重抽只買得到雜訊等級的改善（見同日「圖譜是誰建的」四次量測）。⇒ 全部清掉重來。
- **清掉的**：Postgres 16 張表（節點 4,746／邊 6,499／chunk 432／llm_cache 1,455 等）、
  解析成果 27 份、裁圖與 **173 個人工裁定**、六個試驗 workspace 的掃描入口、
  審核台簿記、原始 PDF（library 32 檔＋inbox 26 檔）。**保留**：模型權重、
  跑著的資料庫目錄、`records/` 的 ledger。
- **資料根搬到一顆 1TB SSD，掛成同一個路徑**（`/data/lightrag`）。逐位元驗過：
  1,844 項 / 7,542,093,603 bytes 兩邊相同。**掛同名是為了不改任何路徑** ——
  compose、`.env`、`DataPaths`、三個 skill 的 URL 一個字都沒動。
- ⚠ **那顆是 USB 外接的**，而 USB 掉了的預設結果是「安靜地寫到別的地方」：沒掛上時
  資料根只是底層磁碟上的空目錄，LightRAG 會**建一個新的空庫而且不報錯**。
  三道守著（掛載點 mode 000／`errors=remount-ro`／`BindsTo` 掛載單元的 systemd 守衛），
  細節與新機器要做什麼在 [rebuild-checklist](../docs/rebuild-checklist.md) 的 A3。
- **`nofail` 保留、docker.service 不綁。** 前者拿掉會讓掛不上時掉進 emergency mode
  而這台只能 ssh；後者會讓 roonserver／samba／nginx 那些**別人的容器**跟著起不來。
- **測失效路徑測出兩個我自己的 bug，都修了。**
  ① `Path.is_file()` 不吞 PermissionError，而沒掛上時掛載點正是 mode 000 ——
  A-34 結論對（hard FAIL）但訊息變成一句 `PermissionError`，把「先不要動工」吃掉。
  ② **A-34 排在「連上容器」之後，根本跑不到** —— 碟不見時容器也連不上，畫面只會說
  「容器連不上」（症狀），真正的原因永遠印不出來。判準抽成 `data_root_state()`，
  `main()` 在連容器前先跑同一份。⇒ **警報要在它該響的那個情境下實測，不能只看它存在。**
- **A-32 空庫時改三態。** 清庫後它拿一份留下來的舊紀錄比對，喊「圖譜是舊規則抽的」
  —— 而那個圖譜已經不存在了。沒有母體時這個問題問不出答案，跟 A-25／A-26 同型。
  過期的紀錄檔一併刪掉。
- **清完的驗收**：`compat-check` hard 0、soft 0、驗不了 3（A-25／A-26／A-32，都是
  空庫本來就問不出答案的），rc=0。四個容器健康。
- 審核台是 systemd 服務、不在那四個容器裡，清庫時沒重啟所以畫面還抱著舊數字 ——
  重啟後歸零。順手把「處理中」提到第二節（PO 指定：進料期間最常看的一節不該在中段）。

## 2026-08-09（六）· 十二道閘門的 V1／V2 接上了，截斷不再靜靜通過

- **接的方式是共用一份實作，不是抄過去。** `vlm.py` 的十二道發現時整包零呼叫點，
  而 apply 那側**重寫**了其中三道 —— 抄一份就是這個坑本身的成因。所以把 V1／V2
  抽成 `vlm.truncation_failures()`，`judge()` 與 `eyes.look()` 都叫它，一份實作、
  兩個呼叫者。測試裡有一條專門斷言「沒有第二份抄本」。
- **斷點就在一行**：`vlm.transcribe` 正確回傳 `finish_reason`、`_store` 正確存進
  快取（21 筆全是 `stop`），但 `_cached` 只挑 `["html"]` 出來，訊號在快取這條路上
  被丟掉。改成回整包之後，**快取命中那條路也驗得到** —— 轉錄是會被長期沿用的。
- **真正會靜靜通過的形狀只有一種，先證明它是真的再補。** 三種截斷實測：單表截斷
  被 `gate_table_html` 擋（但訊息說「不是單一完整的 table」，看不出成因）；表後截斷
  無害；**兩表、第二表截斷會放行** —— `extract_html` 的非貪婪正則只取第一個完整的
  `<table>`，半張表被當成整張表採用，閘門看到的是結構完整的表。測試同時斷言
  舊路徑會放行、新路徑會擋，只斷言後者的話，判準被改回去也看不出損失了什麼。
- **實跑驗證（dker，真實資料）**：21 筆既有快取全部通過，`look()` 端到端呼叫 21 次
  零錯誤 —— 而且 `host` 故意設成 `unreachable.invalid`，真打出去就會回錯誤，
  所以**確定是零 API 呼叫**。拿真實轉錄砍掉尾巴 40 字元則正確擋下並印出該刪哪個
  快取檔。`canary` 27 份全綠（rc=0），基準一個數字都沒動 —— 這次沒碰規則。
- **⚠ 方程式那條路不能套同一個判準。** `eq-check.py` 把 `PROMPT` 換成方程式版，
  輸出是裸 LaTeX，V2 的「以 `</table>` 收尾」對它每一條都會誤判成截斷。三個呼叫點
  傳 `closing=None`（只跑 V1），`PROMPT` 與 `closing` 是同一個旋鈕的兩端。
  這條**NEXT 原本沒寫到**，是查呼叫點時才發現的。
- **被截斷的轉錄照樣寫進快取**（PO 裁決）：壞輸出留在磁碟上可 diff、可重看、
  restic 會備份，下次重跑直接從快取判失敗、不重複付費。代價是快取的鍵不含
  `max_out`，調大額度不會自動失效 —— 所以拒絕訊息直接印出該刪哪個檔、該調哪個鍵。
- 缺 `raw`／`finish_reason` 的快取當成**驗不了**（放行並 log 一行 warning），
  不當成截斷 —— 當成壞了會無端重付一輪轉錄費。實測現有 21 筆都有這兩欄。

## 2026-08-09（六）· 位置標記清乾淨了，標題頁消音上線等重抽

- **提示詞守不住的，改在容器外用確定性的方式守。** 規則 2a（不要把
  `Figure N`／`Equation N` 抽成節點）寫在容器內的抽取提示詞裡，實測三次都沒守住，
  而且**守不住的程度隨後端而變**（受控比對：同規則同解析成果，llama.cpp 與 vLLM
  的人名機構是 35 對 49）。新的 `graph-clean.py` 跑在宿主上、走 LightRAG 的
  `/graph/entity/delete`，**不管抽取跑在本機還是雲端、什麼量化什麼溫度，結果都一樣**。
- **實跑：節點 4,799 → 4,746，位置標記 66 → 27。** 刪掉 53 個，`compat-check`
  的新斷言 A-33 從 FAIL 轉 ok，邊懸空 0 條、向量表孤兒 0 列。
  留下的 27 個是 `region`／`zone`／`mode`／`model`／`part`，PO 裁決先不動
  ——它們可能帶語意，而且與實體碎片化那張審查表是同一批東西。
- **清完再量，才發現樣式漏了一半。** 第一版合併了「羅馬數字」卻丟掉「小數點分節」，
  清掉 39 個之後圖譜裡還活著 12 個 `equation 3.3`（教科書章節的編號）與
  2 個 `figure 5b-d`。⇒ **不是推理出來的，是清完再量出來的**；補齊後多抓 14 個、
  新增待裁定 0 個。
- **樣式只留一份。** 量測（`graph-shape`）、清除（`graph-clean`）、警報（A-33）
  共用 `pp/graph_labels.py`。各留一份的話「清完了」與「還有殘留」會同時成立
  而沒有人發現 —— 與 08-08 審核台那六個洞同一個形狀。
- **標題頁消音：位置只圈範圍，每一項要自己通過測試。** 「從標題消到第一個小標題
  為止」這個直覺做法被真實資料證偽三次：`01701` 與 `C Equivalent Networks` 的
  `lvl=1` 標題後面接的是正文第一段，`2016 - 3D Acoustic Field` 的區塊第 11 項
  是**摘要**。⇒ 27 份實跑：16 份有標題頁區塊、消音 76 項、保留待查 14 項、
  最高比例 3.11%（門檻 8%），三份反例全部落在安全的一邊。
- **金絲雀照出我自己的 bug。** `title_fired=False` 卻 `title_mute=5` —— 第 0 頁
  頁腳那一掃不看區塊，而 `summary()` 看到沒開火就只印「未開火」，那 5 項消音
  在畫面上完全不存在。**消音了卻沒人看得到，比沒消更糟。**
  把 `fired` 單獨記一格正是為了照出這種矛盾。
- **金絲雀基準補回 27 份**（原本只有 6 份，語料早就 27 份了 —— 等於 21 份文件的
  規則行為一直沒人守），並記入原本完全沒被守著的兩條消音規則。
  既有欄位一處都沒變，證明新規則沒有干擾 `layout_noise`。
- ⚠ **標題頁消音改的是餵給模型的文字，圖譜上今天一個節點都不會變。**
  person／organization 仍是 188，效果要等下一次重抽。
- **拆掉一顆地雷：眼睛 A 不再跟抽取 LLM 綁在一起。** 在此之前 `eyes_from_env`
  的眼睛 A 直接讀 `LLM_BINDING_*`，所以改抽取模型會連帶改掉看圖的那一隻 ——
  而 `pp/judge.py` 早就實測過「deepseek-v4 不吃 image_url」。也就是說，哪天把
  抽取換成 DeepSeek，表格轉錄會整條壞掉**而且沒有錯誤訊息**。
  ⇒ 新增 `PP_EYE_A_*` 三個鍵，不設時 fallback 回原本的，**行為逐欄位不變**。
  六條測試釘的全是「沒差」，包括「真的放一個舊檔名的快取進去看撈不撈得到」——
  快取檔名改掉就是既有快取全 miss、重新付一次錢。
  實機驗過：三個新鍵都沒設、眼睛 A 仍等於抽取 LLM、磁碟上的快取檔名
  `c5b5a57e9e19131b.qwen.qwen3.6-35b-a3b.json` 與拆分後產生的樣式相同，
  `compat-check` 維持 hard 0 / soft 1（A-23、A-30 都是 ok）。
- **trial 資料根建好，寫入隔離用指紋證明。** `/data/lightrag-trial/work/` 是
  `parsed`（334 MB）與 `crops`（6 MB）的完整複本，帶 `PP_DATA_ROOT` 就切過去。
  對照組跑 `canary` 通過 27 份。**寫入隔離不是用推論的**：跑一次會寫檔的
  `check --doc 'C Equivalent'`，前後對兩棵樹取指紋 ——
  ```
  跑之前  正式=f9fc50ce9860115a  trial=06c425c153b85791
  跑之後  正式=f9fc50ce9860115a  trial=15f0b33551988ccb
  ```
  正式那棵一個位元都沒動。快取 79 對 79，**沒有新的轉錄，所以沒有付費呼叫**
  ——`crops` 一起複製就是為了這個。
- **順帶照出一份會誤導人的舊檔**：正式的
  `crops/C Equivalent Networks.pdf/review.md` 是 2026-08-07 的，裡面的裁圖路徑
  指向 ADR-0003 已廢除的 `/data/rag/…`，而且說「可修補表格 10 張」。
  trial 這次跑出「1 張」，與版控裡的金絲雀基準（`repairable: 1`）一致。
  ⇒ **是舊檔錯不是隔離錯**。當天稍後重新產生一份正確的（而不是刪掉留個洞）：
  人工裁定與快取的指紋前後相同，所以沒有付費呼叫，指向 `/data/rag` 的行數歸零。
- **`--env-file` 上線，眼睛與文字真的脫鉤了。** 疊加不取代（取代要抄一份完整
  設定，抄錯就多變了一個變數），異動清單一定印且分「覆寫／新增」——
  鍵名打錯時疊加會安靜地成功，實驗結果看起來就像「換了模型但沒差」。
  端到端實跑：覆寫檔把文字換成 DeepSeek、眼睛 A 留在本機那顆，
  `check` rc=0、結果與先前相同，正式那棵樹指紋 `3cab11ef9c32db18` 前後不變。
- **反向對照證明那顆地雷是真的**（不是我講出來的）：
  ```
  有指定眼睛 A    文字=deepseek-v4-flash  眼睛A=qwen3.6-35b-a3b     看得見圖=True
  沒指定眼睛 A    文字=deepseek-v4-flash  眼睛A=deepseek-v4-flash   看得見圖=False
  ```
  ⚠ **拆分不會自動保護你。** fallback 保留舊行為是「今天什麼都不會變」的必要
  條件，所以換抽取模型而忘了設 `PP_EYE_A_*` 時地雷照樣會爆。待辦已記在 NEXT。

## 2026-08-09（六）· 查詢這條路上唯一會抖的那一步，可以搬走

- **正式庫的查詢是不可重現的，而且是現在就有的行為。** 一個沒查過的問題連問三次
  拿回三組不同的段落。成因：`mode=mix` 要走圖譜就得先把問題變成關鍵詞，而那一步
  是 LLM 做的、查詢端溫度 1.0、**而且沒有快取**（`lightrag_llm_cache` 只有
  `extract` 與 `summary` 兩類）。查過的問題看起來穩定是被上層快取住了 ——
  **不穩定只咬「沒人問過的問題」，也就是正常使用的情況。**
- **解法：關鍵詞由外部 agent 給。** `/query/data` 收 `hl_keywords`／`ll_keywords`。
  自帶之後三題各問三次全部相同，分數一題更好、兩題持平、零題變差。
  數日誌證實：**自帶關鍵詞的查詢，llama.cpp 收到 0 次請求**（後端自己抽是 1 次）。
  ⇒ 後端變成零 LLM 的純運算。⚠ 但**不會變快** —— 那次呼叫只花 0.7–0.9 秒，
  5 秒的耗時在向量比對與圖譜查詢上。省的是不確定性不是時間。
- **圖譜值得留：mix 比 naive 好 10.2%，10 題裡 naive 一題都沒贏過。**
  對照之下「圖譜是誰建的」（DeepSeek vs llama.cpp，同 22 份文件）四次量測是
  +1.5%／+0.1%／+0.5%／−0.2%，正負都有。
  ⇒ **「用不用圖譜」比「誰建的」大一個量級。**
- **圖譜不必清到絕對乾淨。** 模型寫的實體描述從來不會到使用者手上（kbapi 只轉發
  chunk），所以垃圾節點最多讓它挑錯段落、不會污染答案。相對地**前端消音
  （參考文獻、標題頁）才是值錢的那一半** —— 那些文字本來會被撈回來佔掉 context。
- 完整的逐步拆解與數字：[cairn/retrieval-architecture.md](retrieval-architecture.md)。
- **這一輪我又踩了一次同一個坑，而且是在寫「不要踩這個坑」的工具時踩的。**
  評分跑完看到「10 題有 7 題分數到小數第四位完全相同」，差一點就當成「兩邊差不多」
  報出去。**那個異常才是真訊號** —— 追下去才發現量測本身不穩。

## 2026-08-09（六）· DeepSeek 試跑：可行、快、但照出比較組的盲點

- **「不思考」是必要條件不是選項。** DeepSeek 官方文件：思考預設**開著**且 effort
  是 high；關掉要送 `extra_body: {"thinking": {"type": "disabled"}}`。實測三次
  思考開著，其中一次 `finish_reason=length` —— 推理把 4,000 token 額度吃光、
  輸出被截斷、**不報錯**。關掉之後快一倍且穩定（3.8–5.9 秒對 7.5–21.0 秒）。
- **LightRAG 送得出去**（我先前判斷錯，以為送不出去）：
  `OPENAI_LLM_EXTRA_BODY` 與 `OPENAI_LLM_TEMPERATURE`，命名規則是
  `OPENAI_LLM_` ＋ 欄位名大寫。容器啟動日誌會印出實際送出的 options。
- **速度**：3 篇 82 秒（預設溫度）／163 秒（temp 0.2）；6 篇含 148 chunk 的
  教科書章節共 201 秒。對照本機 llama.cpp 每篇約 6 分鐘。
- **併發：`MAX_PARALLEL_INSERT` 是文件層閘門**（量到的：3 → 6 之後六份同時跑）。
  但**一份文件內部會平行抽 chunk**（教科書 148 chunk／138 秒，單次 8–10 秒
  ⇒ 約 8.6 條同時），所以 `MAX_ASYNC` 才是真正的旋鈕。
  ⇒ **我先前說「只調 MAX_ASYNC、文件層會擋住」是錯的。**
  16／6 之下：429 零次、重試零次、截斷零次，**天花板還沒摸到**。
- **不要設輸出 token 上限。** DeepSeek 上限 384K，LightRAG 預設不送 `max_tokens`，
  兩輪實跑 `finish_reason=length` 零次。設了才會製造「安靜被切掉」的風險。
  單價（官方）：輸入快取命中 ¥0.02/M、未命中 ¥1/M、輸出 ¥2/M。
- **品質**：對得回原文的比例與其他配置同級（可疑率 DS 1.0%、llama 0.7%、
  vLLM 1.3%），**捏造的關係三邊都是 0**。碎片化比率也同級（7.4–9.1%），
  但**碎法不同**：llama／vLLM 是單複數，DeepSeek 是自創縮寫（`(MPP)`、`(HR)`）。
- **最有價值的副產品：三篇的比較組測不到規則 2b。** `Region I`／`Zone II`／
  `Part 1` 這種標籤只出現在教科書章節，四個配置在三篇論文上全是 0。
  加了 C 那篇之後才量得到：DeepSeek 的 2b 遵守度是 **2／8**
  （做對的長這樣：`chamber (region II)`、`neck (region I)`）。
  ⇒ 而 2b 是**唯一沒有確定性工具能補的規則** —— `graph-clean` 只會刪或不刪，
  不會改名，而那些裸標籤正是 PO 裁決「先不刪」的那一族。
- **修好一支從 2026-08-08 起就跑不起來的檢查**：`extract-check.py` 把
  embedding 模型名直接拼進表名，換成 `BAAI/bge-m3` 之後斜線讓 psql 直接
  syntax error。它只在有人主動跑時才會炸，所以沒人發現。
- **我今天犯的三個錯**（形狀都一樣：拿沒驗的東西當結論）：
  ①「694 個實體名字全部找得到、0 個找不到」—— chunk id 前綴過濾錯了，
  那個 0 是**一次比對都沒做**。真實數字是 10.7%。我當天稍早才引用過
  「乾淨的 0 要先當成量錯」。
  ②「DeepSeek 碎片化比較嚴重」—— 只看一份文件的印象，量三份就不成立。
  ③ 為了測溫度設定跑了 `--force-recreate`，**洗掉第一輪的日誌**，
  於是「82 秒對 163 秒」的差異再也無法歸因。

## 2026-08-08（三）· 重抽驗收通過，審核台不再說謊，六份文件到齊

- **重抽驗收：三條規則一條大勝、一條差一步、一條指標沒動但其實成功了。**
  節點 3,137 → 2,032（-35%），而**這不是崩掉**：掉的幾乎全是
  person／organization（-745）、UNKNOWN（-297）、content（-149），
  而 `concept` **從 962 增加到 1,133**——在多了一份文件（2017）的情況下。
  真正的知識變多了，雜訊被清掉了。
- **規則 1（參考文獻的作者與機構）：793 → 48，-94%。** 四條規則裡最貴的洞補起來了。
- **規則 2a 差一步**：96 → 28，而剩下的 28 個是同一族——`equation 22`、`eq. 7`，
  **全部小寫**。規則舉的例子清一色大寫，句中的 "as shown in equation 22" 沒被
  判定成位置標記。規則已補「不分大小寫」，但**不為了 28 個節點重抽**。
- **規則 3 的指標沒動（89 → 84）但目標達成了**：基準裡的例子是 `Cantwell Wj`
  這種被 Title Case 破壞的作者名，那類已隨規則 1 消失。剩下的 84 組是
  **完全不同的東西**——符號本身的大小寫（`b_0`⟂`B_0`、`c1111`⟂`C1111`）。
  ⇒ 那是實體碎片化的活，而且**比重抽前更需要人判斷**：`b_0` 與 `B_0` 在聲學裡
  未必是同一個量。
- **參考文獻消音的驗收帶控制組**：從 4 份文件各取一條參考文獻的原文字串去查索引，
  命中 0；控制組（真正的正文）命中 1。**0 是真的 0，不是查詢壞掉。**
  五篇論文共 262 條參考文獻，一條都沒進索引。
- **審核台當場抓到在說謊**：同一頁上「已處理 4」與「已進知識庫 5」並存。
  計數那半有過濾狀態、顯示那半沒有——**2026-08-04 學過一次的教訓只補在計數上**，
  而兩段程式碼相距 800 行，沒有東西會發現它們對不上。
  ⇒ 改成分節一律問知識庫，「已處理」直接等於「已進知識庫」那節的長度，
  兩個數字不可能再分岔。
- **「現在不方便」被記成「這份文件壞了」**：2017 那篇 `decision=clean`、
  `reasons=[]`，只因為重抽拿 `inputs/` 當暫存區就被判 failed，而 failed 是死路，
  唯一出口會刪掉 4.0MB 的 MinerU 解析成果。⇒ 守門擋下改成退回「等你看」，
  並補上 `failed → planned` 的重試。**2017 用這條路救回，沒有再付 MinerU。**
- **自動放行補上了**——那是 2026-08-08 早上就下的裁決（`4eacaea`：clean 的不需要
  人確認），當時註明「實作留待下一輪」。我改了狀態機卻只做了另一半，
  被 PO 當場問出來。
- **兩條規則從「沒有人」變成有執行者**：寫死 localhost（AST 判準，不能用 grep——
  19 處命中、0 處違規）、規則改了沒重抽（`compat-check` A-32，接進每日檢查才會
  顯示在審核台，那是唯一的警報管道）。
- **檢索預算側驗完**：四題各餵滿 20 段原文，整包 27–38k／50,000 token。
  **是數量上限在擋不是預算在擋，加預算沒用。** 還沒量的是「答案有沒有比較好」。

## 2026-08-08（三）· 收尾第 3 批：抽取規則定案並啟動全面重抽

- **抽取規則從 0 條變 4 條**，走 LightRAG 官方的 prompt profile 機制（不 fork 映像）。
  之前**沒有任何自訂規則**——參考文獻的作者變成節點、`Equation 5` 跨論文假合併，
  都不是規則寫錯，是**從來沒有人告訴它不要**。
- **規則 2 改成兩級是 PO 的判斷，不是我的**：純指位（`Figure 3`／`Table 2`）完全不
  抽；論文自訂的（`Region II`／`Mode I`／`Configuration 3`）**保留但要帶上它指的
  東西**（`low-frequency absorption region (Region II)`）。它們在單篇裡有意義，
  但同樣會跨論文撞名——兩級把「保留意義」與「避免假合併」同時解決。
  清單是**從圖譜實撈的 96 個節點**，不是憑印象列的（原本漏了 Configuration／
  Part／Mode／Zone／Region，也沒涵蓋 `Figure 1A`、沒句點的 `Fig 3`）。
- **參考文獻要在兩個地方各解一半**：提示詞管「作者不要變節點」，但**管不到「參考
  清單被切成 chunk 佔掉檢索預算」**。後者做成 `pp/rules/reference_section.py`。
  實測全文的 8–23% 是參考文獻，而查詢回來的 20 段原文裡有 10% 是它。
- **「消到哪裡為止」是被真實資料逼出來的**：2017 那篇的參考清單後面接著整份補充
  材料（真內容）。「消到文件結尾」會砍掉 72 項推導與公式**而且不報錯**。
  正解是「消到下一個同級或更高級的標題」。
- **檢索預算三個耦合值一起調**（圖譜上限砍半、`--parallel` 4→2、總預算 25k→50k）。
  原文段數 **5–9 → 20（餵滿 CHUNK_TOP_K）**，而且單次查詢延遲反而從 p50 60 秒
  降到 32 秒——高併發買到的是總吞吐，代價是每個請求都變慢，單人使用時不划算。
- **2017 那篇的頁面尺寸容差修好之後一路通過**（594 vs 595 差 1 點）。但它在放行
  那一步失敗，而狀態機不能從 `failed` 回 `planned`，**要再付一次 MinerU 解析**。
- **重抽前先把基準量下來**：3,137 節點／96 個泛用標籤／793 個 person+organization
  ／89 組大小寫變體。⚠ 793 遠大於 audit 說的 47——那是「跨文件共用」的部分，
  總數佔全圖 **25%**。
- **這一段我犯的錯比做的事還值得記**（完整檢討在 `docs/review-before-reextract-20260808.md`）：
  秘密進輸出 3 次、離開碼抓錯地方 2 次、**自己算而不問系統 4 次**（容器要不要重建、
  設定雜湊、chunk token 數、實體型別查錯表）。
  ⇒ 前兩類已經做成執行前擋得住的守衛（`scripts/guard-command.py`）——**寫進文件的
  規則只在有人記得的時候生效，寫成擋得住的檢查才每次都生效**。
  守衛上線的第一次提交就被自己擋下（commit 訊息裡引用了規則名），那個誤擋也修了。
- **四次「自己算」的共同形狀**：算出來的數字都**看起來沒問題**——時間戳判準四台
  答錯兩台、`config --hash` 漏算 env_file、chunk token 估 600（實際 1,818）、
  型別查錯表回報 person=0（實際 709）。**0 和小數字最危險，它們看起來像
  「這件事不用做」。**

## 2026-08-08（三）· 收尾第 2 批：文件對齊，兩個「大待辦」量完發現一個根本不存在

- **CLAUDE.md 那幾格全錯**（它每開一次 session 就載入一次，錯的成本每次都付）：
  `.env` 寫 54 鍵 6 秘密／實際 61 鍵 8 秘密；`records` 183 檔／實際 10；容器「全部
  移除」／實際 4 個在跑；`nvidia-smi` 壞的／實際好的；embedding 還寫 OpenAI 3-large。
  **處置不是更新成新數字**——那一版一週後又會錯（我當天就寫錯過一次語料份數）。
  改成附量它的指令，並在表尾寫明「這張表刻意沒有數字」。
- **ADR-0005 從此有執行者**。規則在本 repo 決定、實作在 `AI_TOOLS`，中間沒有人守。
  新測試同時檢查 frontmatter 的 `description` 與正文——**`description` 是選用 skill
  之前唯一讀得到的部分**，規則只寫在正文等於「載入後才知道要翻譯」，而那時查詢
  字串往往已經組好了。豁免的 `lightrag-fetch` 要明說豁免，否則「沒寫規則」與
  「刻意豁免」長得一樣。
- **T20（不用 print）那 336 處，逐檔查完發現違規數是零。** 18 個有 print 的檔
  **全部都是 CLI 入口**，print 就是它們的輸出。⇒ **「數字很大」不等於「問題很大」，
  量之前先分類。** 開 T20 只會製造 336 個假警報＋一份要維護的 18 檔豁免清單。
  改成把真正的判準（入口可以 print、模組不行）寫成 AST 測試——**檔案清單會過期，
  判準不會**。
- **ANN（型別註解）用棘輪不用一次清完**：144 個分散在 17 檔、形狀各異，而**寫錯的
  註解比沒有註解更糟——它會說謊，而且看起來像做完了**。先清能證明正確的 42 個
  （compat-check 35＋ledger 3＋judge 3＋coverage-check 1），其餘 102 個逐檔列進
  `per-file-ignores` 並附數量。新寫的碼從此一定要有註解。
- 三個「回 `json.loads` 結果」的函式刻意留著：誠實的型別要嘛是 `Json` 別名要嘛是
  `object`，**那是設計選擇不該在補註解時順手做掉**。

## 2026-08-08（三）· 收尾第 1 批：守門機制 9 條全部上線

- **五條新斷言進 compat-check**（A-27～A-31）。最有價值的是 **A-27**：既有的 API
  斷言全走 `docker exec` 進容器打 localhost，那條路在「容器活著但埠沒綁」時**照樣
  全綠**，而 skill、kbapi 呼叫端、瀏覽器都是從外面進來的。原則早就寫在
  `cairn/testing-restart-policy.md:114`，只是一直沒有執行者。
- **A-31 把 `oracle.mineru_options()` 接上**——那個方法定義了卻**零呼叫端**。
  刻意不把 env 傳給 oracle：要讀的是容器**自己的**環境，那才抓得出 compose 漏傳鍵。
- **部署新鮮度長進 `deploy-stack.py`**（audit 4 明說「不要再開一支」）。順帶發現
  `deploy-stack.py verify` **從來沒有人呼叫過**（該檔檔頭自己寫著「執行者目前是弱的」）
  ⇒ **寫好的檢查沒被呼叫等於沒寫**，這條要升上游。
- **最貴的一課：我自己的第一版檢查會漏報。** freshness 起初用「容器啟動時間 vs
  compose.yaml 最後 commit」當判準，在 dker 實跑後對照 compose 自己的
  `config-hash`，發現**四台答錯兩台**——infinity 誤報成紅、**lightrag 誤報成綠**。
  改用 compose 自己用來決定要不要重建的那個雜湊之後全部吻合，
  `docker compose up -d --dry-run` 獨立驗證同一個答案。
  ⇒ **時間戳只是代理指標。有精確判準時不要用代理指標**，尤其是會漏報的那種。
- **每條新檢查都做了紅燈演練**：餵壞輸入證明會紅、餵好輸入證明會綠。
  沒紅過的檢查不算驗過。
- **ruff 上 pre-commit，碼庫清到零違規**。第一次 `--fix` 拔掉了 26 個
  `# noqa: BLE001`——那標的是「這個 blind except 是刻意的」，是**意圖的記錄**不是
  殘渣，整批退回改成不啟用 RUF100。5 個 `zip` 逐一判斷 strict 而不是套同一個答案。
  清出一個真的 latent bug：`intake.py` 的 `Callable` 沒 import，靠
  `from __future__ import annotations` 才沒炸，`get_type_hints()` 會。
- **不啟用 ruff-format**（實測重排 55 檔）與 **E501**（342 處，中英混排的行寬本來
  就不是一致標準）。⇒ **規則不是越多越好，判準是「抓到的東西是不是真的該修」。**
- **上線當天就抓到三個紅燈**：`lightrag` 與 `postgres` 的設定與 compose 不符、
  `kbapi` 跑著 18 小時前的 `scripts/`。已寫進 NEXT 待 PO 挑進料閒置時處置。

## 2026-08-08（三）· 收尾啟動：逐條驗過 NEXT，範本補回重建會掉的東西

- **PO 決定「清空 NEXT.md」**，25 條依批次重排（守門 → 文件 → 抽取 → 秘密 → 剩下的坑
  → 上游 → **備份最後**）。槓掉「擴到 20 篇」，語料就用庫裡現有的。
- **逐條驗過之後：NEXT 上幾乎一件都沒做**，清單沒有「已做卻還掛著」的問題。真正的
  不一致是另外四種——Qwen3 那條已被 bge 取代該刪、MinerU 到期其實已有 `compat-check`
  A-21 守著、CLAUDE.md 說「容器全部移除」但實際四個在跑、`.env` 鍵數三個文件三個數字
  （54／57／實際 60）。
- **audit 的工單清單漏了一條沒進 NEXT**：實體碎片化（`Helmholtz Resonator` 22 邊 vs
  `Helmholtz Resonators` 15 邊，連 `entity_type` 都判成 artifact／concept）。工具
  `entity-merge.py` 早就寫好了。⇒ **「開了工單」與「進了待辦」是兩件事，中間沒有人守。**
- **`.env.example` 照著重建會把一整天的成果退回去**：缺 `MAX_TOTAL_TOKENS`（那個
  「有料卻說查無資料」的修正）、缺 `LIGHTRAG_DB_ROOT`（冷備份的來源）、缺四個
  `RERANK_*`；而且 Embedding 段還寫著 OpenAI 3-large@3072、`RERANK_BINDING=null`。
  ⇒ **範本不是文件，是重建時真的會被執行的東西**，它過期等於系統會被還原成舊版。
- **發現一條安靜斷掉的路**：`pp/eyes.py:87` 的第二雙眼睛 fallback 沿用
  `EMBEDDING_BINDING_API_KEY`，前提是「embedding 用的就是 OpenAI 金鑰」。embedding
  切本機後前提消失。實測同一把金鑰打 OpenAI **401**、打 `:7997` **200**。
  **改 A 讓 B 失效，而 B 要到下次進料才炸。** 這條要升上游。
- **`.env` 不能 `source`**：`LIGHTRAG_PARSER` 的值含 `;`（實測 `*:legacy-R: command
  not found`）。CLAUDE.md 現行的秘密處理建議正好是「在部署機上 source」——那條建議
  在這份 `.env` 上是壞的。
- 移除兩組沒人讀的鍵（`INTAKE_PORT`、`MATHPIX_*`，grep 零命中），理由保留成註解。
  秘密清單補上 `RERANK_BINDING_API_KEY`、`PP_EYE_B_API_KEY`，加兩支測試守住那張表。
- 驗證：128 passed / 1 skipped ＋ `test_gates.py` 6 案例。鍵名 diff 的「`.env` 有而
  範本沒有」已降到 **0**。dker 從落後 1 個 commit 追到 `45753b5`、落後 0。
- **`PP_EYE_B_API_KEY` 已由 PO 補上並驗過**（164 字，打 OpenAI `/models` 回 200）。
  但**沒有東西在守下一次**——已開新待辦：外部端點的金鑰有效性進 daily-check。
- **查清楚 `ENABLE_LLM_CACHE=false` 是什麼**（PO 問）：它管**查詢**的答案與關鍵字，
  跟管進料抽取的 `ENABLE_LLM_CACHE_FOR_EXTRACT` 是兩件事，上游預設都是 True。
  關鍵在 `operate.py:4317-4333`——答案快取的 key 由問題、查詢參數、關鍵字、模型身分
  算出來，**沒有一項代表「庫裡有什麼」**。⇒ 進了新論文或重抽圖譜之後，同一個問題
  會回舊答案，而且「重抽有沒有變好」會量不出來。維持 false，理由補進 `.env.example`。
  順帶查到：這個 false 是初始部署 `678930e` 就設的，**沒有留下任何理由**。

## 2026-08-08（三）· 四篇進庫、查詢溢位修好、本地 embedding/rerank 定案

- **四篇論文放行進庫**（2019/2020/2024/2025，走審核台，一篇沒卡）。
  圖譜 **1,239 → 3,072 節點、1,995 → 4,289 邊**，chunk 148 → 207。第五篇 2017
  停在「要你決定」（頁面尺寸不一致），沒動。
- **查到一個對外會說謊的缺陷並修好**：查詢組出的 prompt 約 33k，撞上 llama 每個
  slot 的 32,768（`-c 131072 ÷ --parallel 4`），LLM 回 400，而 LightRAG 把它翻譯成
  `No relevant context found for the query.` **知識庫有料卻對使用者說查無資料，
  而且沒有任何紅燈。** 修法是 `MAX_TOTAL_TOKENS=25000`。修前 12 次生成 4 次假「找不到」、
  7 次截斷、1 次完整；**修後 12 次全部完整、零錯誤零截斷。**
- **28000 不夠、要 25000**：預算之外還有系統提示約 3,350 與答案要留的空間。
  我第一次算成 28000，那會讓 400 消失但**截斷繼續**。**兩個各自合理的設定，
  乘除關係沒有任何人在守**——這是這次最貴的一課。
- **`nvidia-smi` 在 dker 其實是好的**，CLAUDE.md 那條「driver/library mismatch」已過期。
  那台有一張 **RTX 2070 8GB、用量 1 MiB**（完全閒置）。**過期的否定敘述比過期的
  肯定敘述更貴**——它讓「本地 rerank」這條路一直被當成不可能，沒人去查。
- **圖譜比對（跨文件連結度）**：131 個共用實體，但拆開看只有約 63 個是領域概念——
  **47 個（36%）是人名機構**（來自參考文獻）、**17 個是 `Equation 5` 這種泛用標籤**，
  後者是**跨論文的假合併**（A 論文的第 5 式和 B 論文的第 5 式被併成同一個節點）。
  另外 `Figure 2` 與 `Fig. 2` 各自是節點，同一張圖分家。
- **naive vs mix 首次量到**（修好之後才算數）：mix 撈回的 chunk **比 naive 少**
  （11–14 對 14–18），重疊 65–88%，六題裡四題 mix 完全沒撈到 naive 沒有的原文。
  圖譜多的是實體關係那一層，觸及 3–5 份文件。
- **拿掉 `Figure N` 實體不會影響找圖**——讀 `kbapi.py:368-395` 才知道找圖是從
  **chunk 內文**用正規式抓的，不經過圖譜。實測「完全不提編號」的查詢照樣回 FIG. 2。
  **先讀實作再爭論**，省掉一輪來回。
- **同一個預設值在不同路徑不一樣**：`max_tokens_per_doc` 在 `generic_rerank_api`
  是 480、在 `cohere_rerank` 是 4096。**看到一個預設值不能假設它適用所有路徑。**
- **我自己踩的兩個**：(1) 兩次把 `.env` 的秘密印進輸出（`LIGHTRAG_API_KEY`、
  `MINERU_API_TOKEN`），兩次都是遮蔽用的正規式寫錯——**「先取出再遮蔽」的設計本身
  就是錯的**，正確做法是列名用 `cut -d= -f1`、用值就在部署機上 `source`，
  讓值沒有機會進入輸出。(2) ssh + docker exec + python 三層嵌套的引號寫錯，
  驅動腳本輪詢整段失效卻沒察覺（狀態一直讀不到，只印「繼續等」）。
- **本地 embedding/rerank 定案**：`BAAI/bge-m3`（MIT）＋ `BAAI/bge-reranker-v2-m3`
  （Apache 2.0），Infinity 一個容器供兩者，跑 dker 的 2070，**由 Dockge 管**。
  rerank 先接（可逆），**embedding 等重新進料時才換**——換了之後新向量表是空的，
  在重灌完成前所有查詢都會查不到。細節與設定對照表在 `docs/audit-20260808.md`。
- **最大的發現不在原本的清單上：中文問英文論文差 63–86%**，而且換 embedding
  救不了（兩個模型落差一樣）。瓶頸在向量檢索，不在「認不認得出來」——rerank 的
  cross-encoder 中文問題對英文正確答案打 0.91。處置是查詢前用領域術語轉英文，
  **完全不用重新進料**。裁決見 ADR-0005，實作在 `AI_TOOLS`（commit `6fc7b9a`）。
- **五種 embedding 並排量過**（3-large 的 3072／1536／1024、bge-m3、3-small）：
  維度差異在雜訊範圍內，1024 就夠；`3-small` 也沒有明顯較差——**「勉強可以用」
  那個印象從來沒被量過**。
- 結論沉澱在 **`cairn/retrieval-tuning.md`**（六條結論、六條教訓，含 chunk 砍半的並排實測）與
  **`cairn/secrets-handling.md`**（秘密外洩兩次的檢討：位置控制全做對了也擋不住，
  「先取出再遮蔽」的設計本身就是錯的）。
- **embedding 也切成本地 BGE-M3，而且只花 45 秒**。關鍵是 chunk 沒動 ⇒ 抽取快取
  （452 筆）全部命中，207 個 chunk 的抽取在 45 秒內走完，只有向量真的重算。
  對照實驗組改了 `CHUNK_P_SIZE` 所以快取全落空、抽了兩小時。
  ⇒ **「換 embedding 很貴」是錯的印象，貴的是動 chunk 大小與提示詞。**
  切換前後：chunk 207／207，節點 3,072→3,137，邊 4,289→4,423（走快取所以實體
  相同，2% 的差異來自合併順序）。切換前有 pg_dump 142 MB，舊的 OpenAI 向量表
  保留著當退路。
- **實驗組整個清掉**：`acoustic` workspace 的 13 張表、第二個 stack、
  `/opt/stacks/lightrag-acoustic/.env`（秘密副本回到只有一份）、`:9622`。
- **決定不改 workspace 名稱**：`_v2` 難看但功能上只是字串，而改它要動
  `backup-cold.sh` 的容器名、`systemd-units.py` 的預設值、三個 skill 的 8 處 URL、
  三個測試檔。波及面大、價值低。
- 完整體檢（26 條問題、PO 兩輪批註、逐條回覆）在 **`docs/audit-20260808.md`**。

## 2026-08-08（三）· 警報管道定案並上線，四篇新論文進來

- **警報管道 = 審核台 `:9710`**（PO 裁決：「只要在 9710 有警告就好，我都會透過那個，
  不需要還要傳訊給手機」）。`docs/NEXT.md` 上卡了一整天的「先決定警報走哪裡」解開。
  實測 `intake.py` 裡 `checks` 出現 **0 次**——紅綠狀態只落在 `checks/latest.json`，
  而**沒有任何人會經過那裡**：當天那個檔寫著 `fail` 已超過一天，沒人知道。
- **最重要的不是 fail 是 stale**：排程停掉後結果會凍住，「一週前通過」跟「剛剛通過」
  長得一樣。過期的綠燈比紅燈危險。五種狀態 ok/fail/stale/missing/unreadable，
  **只有 ok 不顯示**——常態佔畫面會淹沒真的紅燈。
- **上線第一次就報了三件事，全部查清楚**：systemd（兩個 timer disabled，就是要開的）、
  canary（新增 4 篇消失 11 篇，語料整個換掉）、compat-check（**1 項不是 46**，
  我第一次猜錯欄位名——`status` 實際是 `ok`。**猜欄位名跟猜結論一樣危險**）。
- **PO 隨即要求暫停 cold-backup**：`backup-cold.sh` 當天改過 DBS 卻沒跑過，而它會停
  容器。於是加了 `PAUSED` 機制——**刻意暫停要跟「沒 enable」分開報**，否則警報管道
  第一天就開始訓練人無視紅燈。反向也擋：列在 PAUSED 卻 enabled ⇒ 有人開回來沒清掉。
- **秘密外洩並修復**：`oracle.py` 用 `-e KEY=VALUE` 傳設定，逾時那條 `raise` 印完整
  指令 ⇒ 六個秘密一次全出來；當天實際外洩過 `LIGHTRAG_API_KEY` 與 `POSTGRES_PASSWORD`
  （**PO 應更換**）。改用 `--env-file`（0600、用完即刪）＋ 錯誤訊息遮蔽，八條測試守著。
- **四篇新論文解析完**：2020／2024／2025 乾淨，**2017 被 `preflight` 擋下**——頁面尺寸
  `{(594,842),(595,842)}`，22 頁裡前 14 頁與後 8 頁差 1 點。檢查是對的（尺寸混雜時
  bbox 換算會錯且不報錯），但它是精確比對，分不出「差 1 點」和「A4 混 A3」。
  `compat-check` 的 A-14 抓到同一件事。
- **oracle 問到容器實際的解析選項**：`is_ocr=True`、`model_version='pipeline'`——
  所以那四篇的解析是對的。但那是人手動跑的，`rebuild-checklist` A 節六條仍無執行者。

## 2026-08-08（二）· 收件匣審核台上線

- **`intake.py` 對新架構是活的**：22 個測試通過、實跑起得來、端點全通，
  `/api/state` 的 `convergence.processed: 1` 認得我們灌的那篇。已 `enable --now`，
  開機會自己回來（`:9710`）。
- **早上把它分類成「入庫自動化的狀態機、這輪用不到」是錯的。** 它是**收件匣／
  進料審核台**——拖 PDF 進去、看候選狀態走完管線。而且它的流程把修補排在送進索引
  之前，**正好擋住「拖進 LightRAG WebUI 會跳過後處理」那個問題**。
- **它刻意不進 compose，理由寫在單元檔裡且值得記住**：審核台會呼叫 `postprocess.py`，
  而 `oracle.py` 靠 `docker exec` 問容器。容器裡要做這件事就得掛 docker socket ——
  而它是**對外開埠、接受檔案上傳**的網頁服務。**給它 docker socket 等於誰打進來誰就
  有整台機器。**
- **又抓到一句自己寫的誤導訊息**：`install --only` 印「檔案已寫入但沒有 enable：…」，
  但清單裡的 `lightrag-stack.service` 上一輪就 enable 了 —— 同一句話涵蓋「這次沒動」
  與「現在是停用的」兩個完全不同的狀態。改成逐一查真實狀態再講。
- 現在的單元狀態：`stack` 與 `intake` enabled，兩個排程仍 disabled（等警報管道的裁決）。

## 2026-08-08· 重開機自動復原修好並驗過（零人工介入）

- **`lightrag-stack.service` 裝上、第二次重開機實測通過**：`ExecStartPre` 等到 Tailscale
  位址、`ExecStart` 拉起 stack，12/12 容器回來、埠綁上、節點 1,239 邊 1,995、端點全通。
  **判準是「重開之後不碰它，服務自己好」**，不是「重開之後我修好了」。
- **`systemd-units.py install` 加 `--only`**：原本會無條件啟用整個 ENABLE 清單，
  包含兩個「等警報管道決定之後才開」的 timer 與 intake。為了裝一個新單元順手開三個
  沒講好的東西，那是「順手做了沒講」。跳過的會明確印出來。
- **dker 裝了 pytest**，109 個測試第一次在那台真的跑過（之前 daily-check 每天報
  「測試失敗」，實際是根本沒跑）。
- 前一則寫的「不用真的重開機也能驗完」**被同一天的實測推翻**，更正寫在
  [testing-restart-policy.md](testing-restart-policy.md)：把大測試拆成小測試時，
  要問**拆掉的縫裡有什麼**——這次縫裡正好就是缺陷。

## 2026-08-07（九）· 補洞：deploy 守衛、自動復原、一個說謊的訊號

- **`deploy-stack.py`**：repo 的 compose 與 Dockge stack 那份不得漂移。verify/diff/install，
  比對與安裝共用同一個讀取實作（理由同 `systemd-units.py`）。dker 實測一致，
  `sha256:d434673f3e6df381`。⚠ 執行者仍弱——只有人跑它時才執行。
- **自動復原：殺程序過、重開機不過。** 從宿主殺容器主程序 → `RestartCount` 0→1、
  47 秒後 healthy、資料完好。我據此寫下「不用真的重開機也能驗完」——**那句是錯的**，
  PO 授權後真的重開，露出一個殺程序測不到的缺陷（見下一條）。
  過程中兩次假警報的方法論寫進 [testing-restart-policy.md](testing-restart-policy.md)。
- **重開機我們的服務不會回來（真缺陷）**：`failed to bind host port 100.87.88.7:9621:
  cannot assign requested address`。開機時 docker 比 tailscale 早起，而我們刻意綁
  Tailscale 位址（`:9700` 完全沒有認證，綁 `0.0.0.0` 等於知識庫在區網裸奔）。
  綁定失敗是**啟動失敗不是程序死亡**，restart policy 救不了。同機別人的 10 個容器全綁
  `0.0.0.0`，所以只有我們踩到。修法：`lightrag-stack.service`，`ExecStartPre` 等位址
  真的出現（`After=tailscaled` 不夠，那只保證 unit 起了不保證位址已指派）。
- **`docker compose up -d` 救不回失敗的容器，要 `--force-recreate`。** 這條最陰險：
  `docker compose ps` 顯示 `running`、`docker port` 卻是**空的**，外面完全連不上。
  我因此誤判過一次「服務已救回」。**`ps` 說 running 不等於服務可用。**
- **抓到一個一直在說謊的訊號**：dker 的 daily-check 輸出「測試失敗：pytest rc=1」，
  但那不是測試失敗，是**那台根本沒裝 pytest**。兩件完全不同的事長成同一句話，
  紅燈落在 `/data/lightrag/checks/` 沒人看得懂——而排程停用、沒有警報，也沒人去看。
  `run-tests.sh` 改成先問裝了沒，沒裝就說「驗不了」並回 exit 3（回 0 會讓「只驗一半」
  看起來像「全部通過」）。
- ⚠ **dker 不能跑 pytest 這件事還沒解**，只是不再說謊。要裝要 PO 決定。
- commit 訊息寫錯一次測試數（109 vs 實際 108+1 skip），**push 前發現，用 amend 改掉**
  ——推出去之後就只能追加了。

## 2026-08-07（八）· 一篇打通全程完成，關係數 1,995

- **抽取 40 分鐘跑完，節點 1,239 / 邊 1,995。** 設計 D3 的斷言「關係數不是 0」以最大
  差距通過——邊比節點多，是健康的圖，不是一袋沒有連線的名詞。
- **後處理有效**：空表格 9 → 1、修補 73 項、消音 146 項（`_pp_original_*` 可還原）。
  `doc` 端點回傳時看得到「已修補」標記。
- **D6 那個沒人量過的問題有答案**：本機模型抽實體關係時**沒有捏造**。可疑 3.0% 逐一
  看內容，全是「自己加型別前綴」（`Coefficient B0`）與 KI-003 的羅馬數字誤讀。
  它在表格轉錄會編 imgur 網址，在抽取層不會——**兩層行為不同，不能互相推論**。
- **五個端點全通**，三個 skill 一行沒改就能用。
- **掉字 10.6% 維持 fail，但那是重現**：歷史 10.5%（381/3629）vs 今天 383/3629，
  差 2 個詞。成因 2026-08-02 已逐詞歸因完。
- **補上一個沒接的執行者**：`verified-findings.json` 的規則寫「檢查工具在指標超標時
  **自動印出**已查證紀錄，不要求任何人記得去哪裡找」，但只有 `extract-check` 接了，
  `coverage-check` 沒有。我因此又查了一次 ledger——正是那條規則要防的事。已接上並補
  前例，實測會印。
- **`.env` 從五份收到一份**：repo 那份刪除、`/data` 的 8/4 備份與 repo 裡兩份 8/3 備份
  刪除，只剩 `/opt/stacks/lightrag/.env` 與一條指過去的 symlink。
- **`intake.py` 早上分類錯了**：我當它是「入庫自動化的狀態機」說這輪用不到，實際它是
  **收件匣／進料審核台**（:9710 的網頁，拖 PDF 進去、看候選狀態走完管線）。它的流程
  把修補排在送進索引之前，正好解決「拖進 LightRAG WebUI 會跳過後處理」那個問題。
  決定：先有基準再接，接之前拿它的測試對新架構跑一次看壞在哪。

## 2026-08-07（七）· 解析重跑，人工裁定 10/10 對得上

- **解析 43 秒、556 個項目、`is_bundle_valid` 通過**。基準數字全中：項目 556（基準 556）、
  表格 57（57）、空表格 9（9）。
- **10 個不可再生的人工裁定全部對得上**——索引仍是 `table`，頁碼與 `review.md` 記的
  一致。**鐵則第 8 條警告的錯位沒有發生**，重判成本是零（早上估的最壞情況是重判 10 張）。
  ⚠ 一份文件的證據，擴到 20 篇時每份都要重驗。
- **先驗 PDF 的 sha256 再談索引**：`1c7dcb0e1de0393c` 與 ledger 記的一致。順序不能顛倒——
  不是同一份檔案的話，索引對得上也沒有意義。
- **`.env` 搬家踩出一個靜默失敗**：`mineru_common.load_env()` 在檔案不存在時**回空字典
  不報錯**，所有腳本會拿到「沒有任何設定」繼續跑。先用 symlink 接回（刪 repo 只刪連結，
  不碰秘密本體），`load_env` 該改成大聲失敗——已列進 NEXT。
- **`EMBEDDING_SEND_DIM` 缺席是對的**：它管截斷，而 `EMBEDDING_DIM=3072` 等於
  `text-embedding-3-large` 的原生維度，不需要截斷。`rebuild-checklist` 寫成無條件必要，
  那個寫法會誤導，要改成有條件的。

## 2026-08-07（六）· 系統起來了：v1.5.6、圖進 Postgres、Dockge stack

- **`PGTableGraphStorage` 從推測變成實跑**。三層都驗過：映像清單有它 → 啟動 log
  `[acoustics_v2] PGTableGraphStorage initialized` → Postgres 裡真的建出
  `lightrag_graph_nodes` 與 `lightrag_graph_edges`。擴充只有 `plpgsql`＋`vector`，
  **沒有 age**，確認不需要 Apache AGE。13 張表全在同一個庫。
- **v1.5.5 沒有這個選項**（問映像本人），而 compose 原本釘的 digest 就是 v1.5.5——
  升級是必要條件不是順便。v1.5.5 映像已從 dker 移除。
- **三個容器 healthy**：lightrag（1.5.6）、postgres、kbapi。`:9700` 與 `:9621` 都回應。
- **`.env` 搬出 git checkout** → `/opt/stacks/lightrag/.env`。刪 repo 不再連帶弄丟秘密。
  順帶清掉死鍵 `NTFY_URL`（ntfy 今天拆了，全 repo 零讀取端）。現在 54 鍵。
- **數錯一次並更正**：`^[A-Z_]+=` 配不到含數字的鍵名（`NEO4J` 的 `4`），少算 4 個，
  錯的數字進了 `e39d6d4`。用追加更正不 amend——已推送。
- **kbapi 的定位釐清**：它不是薄代理，是**兩個 skill 的唯一來源**——LightRAG 沒有
  「這篇有哪些表格／公式／圖」的端點，那些資料在 MinerU 產物裡，它從沒讀過。
  PO 當初做它的主因是「只開一個埠」，但 `:9621` 一直也開著，**目標其實沒達成**；
  暫時保留（WebUI 有圖譜瀏覽器），決定寫進 NEXT。
- ⚠ **庫是空的，抽取一次都沒跑過。** 「能建表」不等於「能寫入」。

## 2026-08-07（五）· 清理 coder

- **刪掉六個前提已消失的檔**（約 1,960 行）：`compare-ws.py`（比較對象 v155 不存在了）、
  `llm-bench.py`＋測試（選型已定案）、`mathpix-test.py`＋`pp/mathpix.py`（第三隻眼實測
  選的是 MiMo）、`askrag.py`。不搬 `archive/` 目錄——歷史在 git 與 tag 裡。
- **`archive-ledger.py` 原本也在清單上，查證後撤回**：重建後庫裡只有 1 份文件、
  成績單有 20 張，19 張立刻脫節，而 `ledger.py` 那時會拒絕輸出總表並叫人跑這支。
  **清單靠自述判斷不夠，要查誰在引用它。**
- **skill 定位裁決**：三個 lightrag skill 住 `AI_TOOLS/skills/common/`，repo 副本刪除。
  判準不是「是不是本專案專屬」，是**誰用它**——Obsidian 的 agent 不在這個 repo 底下。
- **修掉一個每個 session 都在說的謊**：`lightrag-search` 的 `description` 寫
  「20 parsed papers」，那個庫已移除。`description` 是強制載入的，所以每次開場都灌一次
  假前提。三個 skill 都加上「重建中、連不上就如實說」（`AI_TOOLS` 的 `2c75ace`）。
- **`verdicts/README.md` 更正**：不再說 173 個全部不可再生，見
  [irreproducible-claims.md](irreproducible-claims.md)。
- **設計文件更正**：`extract-check.py` 早就在做「實體名能不能在原文找到」的檢查，
  原本寫成待做。
- **新長出三條上游畢業候選**，寫進 `docs/NEXT.md`，不在這一輪做。
- commits：`7a0414b` `b0c8d7d` `c7186f5`。

## 2026-08-07（四）· 重建需求釘死、文件治理收口

- **需求拷問完成，裁決在 `docs/rebuild-design.md`**（八個決定）。定位是「原料供應站」：
  不下結論，把乾淨原料交給 Obsidian 的 agent。範圍縮到 `C Equivalent Networks.pdf`
  一篇打通，手動分步跑。
- **拷問推翻了草稿自己的三條假設**：skill「一行不改」等於凍結舊拓樸（改成凍契約不凍
  位址）、`doc` 端點根本不回正文而 `search` 不能鎖定單篇（新增分節正文端點）、
  227 個「不可再生」實際只有 10 個。
- **第一個 topic note 誕生**：[irreproducible-claims.md](irreproducible-claims.md)。
  Cairn 的核心機制第一次被啟用——上一則還寫著「仍是 0 個」。
- **文件治理**：`AGENTS.md` 72 行 → 8 行純指標（上游範本本來就是一行 `# See CLAUDE.md`，
  是本專案偏離了）；獨有內容進 `docs/knowledge-routing.md`；`NEXT.md` 補 BASELINE 1.4.0
  要求的狀態總表與 mermaid 進度圖。**兩份導航表已各缺對方的項目**，後果是用 codex 的人
  從來不會被告知有 8 條鐵則。
- **兩支執行者掛上 pre-commit**：LOG 不得落後於 git、NEXT 的 done 不得追上待辦。
  原本 `test_log_freshness.py` 的執行者是 dker 的 timer，實測 `systemctl list-timers`
  只剩 apt-daily 兩個——規則有、執行者沒了。
- commits：`ffe7d12` `7b0cc58` `e2cfdc8` `7e16abf`。

## 2026-08-07（三）· 規範層：BASELINE 1.9.0 → 2.0.0

- **本專案的事故升格成跨專案規則。** 從 173 條踩坑清單提煉五條提案，請 fable 唯讀評估
  （97k tokens／23 次工具呼叫），四條採納、一條的藥方被它否決。
- **BASELINE 2.0.0**（`standards` 的 `f3f5e30`）：核心第 9 條加強、新增「規則的執行者」
  與「量測紀律」、提交紀律加「不得 amend 已推送」。指紋 `d31afca4…` → `f2d0bcfa…`。
- **上游體檢 11 項全部修完**，其中 **5 項現在是機器在守**（`standards/scripts/self-check.py`
  接在 pre-commit：frontmatter／指紋／範本 snapshot／type 清單／死引用）。
- **根因是「只有 BASELINE 有人維護」**：它改過十次，其餘八個檔自 2026-05-23 遷入 git
  後一個字都沒動 ⇒ 範本出貨「5 條」私貨、版本號凍三個月、引用一個不存在的 skill。
- **PO 定位自己是「系統外審查」** ⇒ 寫進 BASELINE：人的審查不算執行者，
  `沒有人` 不因為「反正會被發現」而降級。
- 本專案側：snapshot 同步兩次、`NEXT.md` 搬到 `docs/`、拆掉 ntfy、解除 Dockge 對 repo
  的 bind mount（那條掛載會讓 UI 的刪除按鈕刪掉 repo 本身）。
- 詳細理由與條文在 `standards/BASELINE.md` 與 `standards/CHANGELOG.md`。
  ⚠ **`cairn/<topic>.md` 仍是 0 個**——今天新長出來的知識（工具層與規範層怎麼分工）
  還沒定案，等 fable 評估後再建 topic，不先寫成要改的東西。

## 2026-08-07（下半）· 清空、砍文件、拆通知

- **dker 清空**：四個容器移除、`/data/lightrag` 只留 `records` 183 檔與 `checks` 32 檔
  （釋放約 2 GB）。凍結點 tag `archive/pre-rebuild-20260807`。
- **repo 砍 9,179 行**歷史文件（5 份文件＋173 條坑清單）。提煉出
  `docs/rebuild-checklist.md` —— 173 條裡只有 13 條在乾淨重建後仍成立，
  **其中 8 條的執行者是「沒有人」且壞掉不報錯**。
- **CLAUDE.md 655 → 115 行**。PO：「開一次就呼叫一次耶」。只留機器關係與藍桶 9 條；
  鐵則與領域知識移到 `docs/hard-rules.md`（沒刪）。每 session 載入從 715 → 164 行。
- **拆掉自架 ntfy**（容器、目錄、`notify.sh`、兩個 OnFailure 備援單元）。
  ⚠ **「誰會報錯」的答案又變回「沒有人」** —— 重建後要先決定警報走哪裡。
- **解除 Dockge 綁定**。dockge 的 compose 有一條把 repo 掛進 `/opt/stacks/lightrag`，
  **UI 的「刪除」會刪掉 repo 本身（含 dker 上唯一的 `.env`）**。移除掛載、重建 dockge、
  驗證 `.env` sha256 逐位元相同（`cb5b4742593d6c96`）、repo 328 個檔一個沒少。
  ⚠ 那個改動在 dker 的 `/opt/stacks/dockge/compose.yaml`，**不在版控**。
- **PO 連續抓到我三個錯誤**，全是「文件說的」與「實際的」不一致：coder 上其實有
  `.env`（llama.cpp 的）、Neo4j 還被寫成現況、workspace 名稱該改。
  加上我自己犯了三次同型錯誤（字串比對沒看內容就下結論）。

## 2026-08-07 · 四件補牆＋一個診斷：規則沒有執行者

- **`/data/rag` 廢除**（PO 定案）。兩處活的依賴改掉：冷備份暫存區搬到
  `/data/lightrag-coldstage`（**不能放資料根底下**，`cp -a "$DB_ROOT/." "$STAGE/"`
  會複製進自己）、`INTAKE_SOURCES` 留空。PO 清掉目錄後 03:28 又出現一個空的
  ——那是備份腳本自己 `mkdir` 的，刪一次建一次。
- **README 備份表三列全錯，方向相反**：打 ❌ 的 postgres／neo4j 每天備兩次，
  打 ✅ 的 `/data/rag/lightrag` 整個不存在，容量還錯一個量級。這張表**連續錯兩次**
  （08-03 之前寫「已納入備份」而沒有；修正後停在舊路徑）。
- **`apply` 加鎖**：`FORCE_REPARSE` 開著時拒絕執行。工單 830 行早就裁決要做，
  一直沒做。判準收斂到 `pp/oracle.py`，`A-07` 改用同一個函式。三條進料路徑
  都收束到 `cmd_apply`，一道鎖擋住全部。
- **`ledger.py summary` 自我停用**：它印「151 項通過」，那些全是 08-04 換掉的
  舊語料；現役 18 份幾乎一格都沒驗。dker 實跑 rc=3，指名 15 張幽靈。
- **不可再生的人工裁定進版控** `verdicts/` 227 檔 1.3 MB（NEXT 那條躺很久的待辦）。
- **決定重建方向的數字**：坑清單 173 條裡 **151 條在乾淨重建後不存在**；
  那 21 條「沒有人守且高優先」的裡面**重建後只剩 2 條**（都是 `.env` 的值）。
  ⇒ 補探針那條路的價值在重建那天歸零。
- **診斷（PO 追問「一套方法」逼出來的）**：`standards/BASELINE.md` 與 Project Cairn
  設計都完整，但**執行者都是「沒有人」**——掃描腳本預設路徑還是 Windows 的 `E:/`、
  沒有排程、standards 自己的 git hook 是空的、9 個 `.md` 零個有 frontmatter、
  `NEXT.md` 799 行（標準 <80）、**我今天 6 個 commit 全部不符合它的 commit 格式**。
  Cairn 只實現了 LOG.md，知識專題文檔 0 個、`Cited.md` 不存在。
  ⇒ 本則之後接三件執行者：LOG 落後檢查（本則就是它逼出來的）、pre-commit、NEXT 瘦身。

## 2026-08-05 · 整本書跑完，文件對齊現況

- **Möser A–R 十八章全部進知識庫**（1,589 chunk、14,226 實體、26,447 關係），
  收件匣清空，一份都沒卡在「等你看」。G–R 那 12 份的公式從一開始就是對的
  ——σ̂ 規則在跑 G 之前就落地了，不像 B/D/E 要事後重跑。
- **全套體檢**：canary 1 秒 ✅／compat-check 124 項 ✅／extract-check 2.7% 可疑率。
  `A-25` 從「驗不了」自動轉回真實判斷（母體長大了），不必改任何一行程式。
- **新機制：`tests/verified-findings.json`**。起因是 PO 指出「需要問就是規則沒寫好」
  ——我問了「這個判定要記到哪」，而那個問題本身就是缺陷訊號。查下去發現
  規則有、路由沒有：六個地方可以放而沒說怎麼選。**同一個查證已經做過兩次**
  （K Muffler 舊語料查過、L Capsules 今天又查一次）。
  解法不是「寫進某個文件」，是**讓檢查工具自己在超標時印出前例**。
- **`AGENTS.md` 補路由表**，判準是「誰在什麼時候需要它」——時機不會吵，分類會。
- **踩到一個雙 checkout 分岔**：先 push `8cf0c0a`、dker pull 走，之後 `--amend`
  補實跑輸出成 `23312c5` 並 force push ⇒ dker 抱著一個遠端已不存在的 hash，
  `pull --ff-only` 直接失敗。驗過兩個 commit 的檔案內容完全一致才 reset 對齊。
  **這次 git 有報錯是運氣**，一般的雙 checkout 分岔是靜靜分家的。
  規則已補進 CLAUDE.md 的「提交紀律」：**推出去之後不得 `--amend`**。
- CLAUDE.md 的「現況」與接地檢查一節更新到 18 份的實測數字；
  NEXT.md 加 2026-08-05 交接節。**舊語料的 20 份／7,211 實體不再可當現況引用。**

## 2026-08-04 · 七條跨專案知識畢業到 Obsidian

- 畢業 7 條 + 1 份索引到 `42_Cairn/lightrag/`，走 WebDAV 直寫，全部逐位元回讀驗證。
- 條目：乾淨的 0 要先當成量錯／先查輸入再查偵測器／探針要在沒人問的時候會響／
  門檻用量的不要用調的／認結構不認字元／對帳要問跑著的系統／設定只活在一台機器上。
- 索引開頭寫明它們是同一件事的七個面：**畫面說的跟實際發生的不一致，而且都不報錯。**
- 前四條來自 `CLAUDE.md` 的鐵則，後三條是 2026-08-04 這一輪長出來的
  （commit `c94d2fe`、`269653b`＋`2e7e2bb`、`42c73ef`＋`5dca4c8`）。
- **上傳時真的踩到自己寫的那條**：〈乾淨的 0〉那篇 PUT 回 `000`——不是 HTTP 碼，
  是 curl 因檔名含空格未編碼而根本沒送出。七個 201 配一個 000，看起來像「大致成功」。
  修 URL 編碼後重驗，八個全 200。

## 2026-08-04 · Project Cairn 初始化

- 初始化 Project Cairn 結構：`AGENTS.md`、`.cairn/config.yaml`、`cairn/LOG.md`。
- 歷史遷移模式：`selective_migrate`。**既有的 4,031 行文件不搬進 `cairn/`** ——
  `docs/precedents-inventory-20260804.md` 已把裁決盤點成 75 條，但抽驗 5 條錯 3 條，
  它自己的結論就是「不能自動匯入」。selective 的範圍只有「跨專案可複用的七條直接畢業」。
- 兩個獨立審查（codex luna xhigh、deepseek v4-pro）都判 **C：只遷移最高信心子集**。
  luna 另外指出 cairn 補的是「agent 查知識」的消費者，**不是「程式查先例表」的消費者**，
  混為一談會產生假完成感 —— 所以機器可讀的先例表仍是未做的事，不因 cairn 落地而消失。
- Obsidian provider 走 **WebDAV 直寫**（vault 在 NAS、本機無掛載亦無 obsidian CLI）。
- 細節見 `AGENTS.md` 與 `.cairn/config.yaml`。
