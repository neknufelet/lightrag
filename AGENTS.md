# lightrag

**所有規則、契約、跨機座標、導航全部在 [CLAUDE.md](CLAUDE.md)。去讀那個。**

這個檔存在的唯一理由：codex／opencode 這類 CLI 讀 `AGENTS.md`，不讀 `CLAUDE.md`。

**它刻意不放任何內容。** 放了就要同步，而同步會漂移且不報錯——2026-08-07 實測：
這裡曾有第二份導航表，與 `CLAUDE.md` 那份**互相缺對方的項目**，後果是用 codex 的人
從來不會被告知有 `docs/hard-rules.md` 的 8 條鐵則，而那份自己標著「動 `scripts/pp/`
之前必讀」。判準與搬家紀錄在 [docs/knowledge-routing.md](docs/knowledge-routing.md)。
