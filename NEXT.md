# NEXT — 待辦與進行中

規則與契約在 [CLAUDE.md](CLAUDE.md)（SSOT），重建的階段與閘門在
[docs/rebuild-plan.md](docs/rebuild-plan.md)。這裡只放「接下來要做什麼」。
做完就刪——教訓沉澱回文檔，不留屍體；新增時寫清楚證據在哪。

## 進行中：acoustics_v2 重建

階段 0–2 已驗收（2026-08-02）。寫回完成（消音 520、C 表格 10 修、
chart→image 184、C coverage 16.1%→13.1%），紀錄見 rebuild-plan 階段 2。
**階段 2 的改動未 commit**（worktree 4 檔＋v1 的 daily-check.sh）。

### 已決策與進度（2026-08-02 晚，收尾工單執行中）

- **∂ 整族**：第一輪 983＋40 處已套用驗收；封閉掃描攔下第二輪 57 處變體，
  主線親驗裁圖後 51 處定案（35 機械＋#942 整條重轉錄＋盲點修補）。
  四條證實截斷的巨型式：#947／#555／#957 已重轉錄，#942 執行中。
  定案審計軌跡：`records/review/partial-family.md` §8、`partial-family-r2.md` §8。
- **C 的「b」前提被量測修正**：12.0% 裡表格流失 163 詞中 72% 是**黏字**
  （`ResistorCapacitorCoil`、`\mathsf{t a n h}`——內容在、分隔符掉），
  真缺只 46 詞。已定案執行中：兩處閘門窄改（gate 容許既有本地 img／
  curated 只加不改路徑）＋五筆補格（≈39 詞）＋`\times` 誤讀機械修補
  （viix 真相：下標 x 被讀成 ×）＋格內逐字母排版正規化。
  定案：`records/review/c-tables-disputes.md` §7。
- **41598 waiver 已記**（政策性已知遺失，使用者 2026-08-02 核准）。
- **qwen 的 `<img>` 捏造已規模化證實**（57 張表中 24 張、8 張 imgur）——
  記入 model-observations；閘門的存在理由再次被驗證。
- **待辦（收尾工單回報後）**：C 的 93 個「bbox 未覆蓋」詞歸因 → C 最終
  waiver 決策（使用者）→ 階段 3 抽取工單。
- **解析階段議題（擴量前再議）**：表格結構黏連（rowspan 塞多列無分隔）
  無法在後處理層修，屬 MinerU 上限。

### 階段 2.5 候選工單（發 Opus，等上面決策）

- [ ] coverage-check 補 LaTeX 字母間隔正規化（`\mathrm{w i t h}` 型，
      與 ﬂ 合字同族假訊號；N Flow 8.7%→7.0%、C 13.1% 會再降）。
      改偵測器要 20 份全部重量、體檢表 note 註記偵測器版本
- [ ] 41598 #41 空 text 項的「文字層回填」規則（機械可窮舉：單一項、
      bbox 已定位、原文在文字層零歧義）
- [ ] N Flow #1135/#1518 巨型 equation 吞散文的處置（判不準，
      證據攤在體檢表 note）
- [ ] parse-check 讀回 `_pp_original_text`（消音後空塊 11→111 的假訊號；
      同一個 bug 兩支腳本只修了 coverage-check 一支）
- [ ] ∂ 整族的分類材料：每處帶前後文的清單，供人裁決
- [ ] eq-check 三票多數決（付費）→ `pp.equations` 從 unverifiable 轉正
- [ ] cmd_apply 批次原子性（單份失敗回滾；目前靠 dry-run 全過當前置閘門）

### 最優先（階段 3 之後第一張工單）

- [ ] **封閉掃描器進版控，變成會自己響的探針**。現況：它是 scratchpad
      裡的拋棄式腳本，兩輪都是如此——**上一輪的盲點因此活過一整輪，
      這輪又找到第三個**（accent 被包在字體命令裡而看不見）。
      這是「新符號誤讀」唯一的偵測手段：漏字檢查抓不到它（誤讀不刪字，
      覆蓋率永遠 100%）、preflight 也抓不到（型別沒變）。
      規格：以**位置**為錨（accent 類 ∪ 裸花體 token 站在算子位置而非
      `\partial`），不以符號枚舉；殘留清單白名單化（c̄、D̄、ρ̃、`\mathfrak{O}`
      孔隙率、大 O 等已定案真符號），**冒出白名單外的 token 就報**。
      進 compat-check 或獨立閘門，納入 daily-check。
      校準教訓：錨點放太寬會炸出 2,803 處合法符號——**永遠很長的殘留清單
      就不是殘留清單**。
- [ ] N Flow #1410 `\mathbf{\overline{\partial}}` 單處缺陷（overbar 被
      MinerU 掛到 ∂ 上，應為系綜平均的範圍問題）——屬重轉錄不屬換記號

### 階段 4／5：驗收與切換（下一張工單）

階段 3 已完成並經主線獨立驗收（2026-08-03）：20 份 processed／0 failed、
A-25 自動轉正、體檢表 160 格全滿、v2 對 v155 掉字 chunk −69%。剩下：

- [ ] **kbapi 指向 v2**（compose 的 9700 要先變成變數）、三個 skill 換位址
- [ ] v155 退役（先停容器觀察數日再刪資料；`__parsed__` 快取值錢，備份先確認）
- [ ] 分支 `rebuild/acoustics-v2` 合併回 master
- [ ] 六份接地 >5% 的處置：**已查明不是幻覺**（符號→概念命名族 ＋
      概念→引用文獻族）。前者的根因是 `SYMBOLIC_RATIO=0.35` 把同一族
      切成兩半——調門檻前先照鐵則 5「看差在哪些具體記號」
- [ ] DeepSeek 判「符號型／驗不了」那 1,482 個（v2 實測值）：
      先抽 50 個由主線標答案當考卷，跑「普通／普通＋加大脈絡／推理」
      三組比正確率與成本，再決定用哪種
- [ ] **MTP 加速評估**（`--spec-type mtp --spec-draft-n-max 3`）：
      Qwen3.6-35B-A3B 原生支援、實測 1.7–2.5×，且它「強制 n_parallel=1」
      的限制對我們免費（本來就 1 slot）。三關：GGUF 有沒有帶 MTP 權重、
      GPU0 只剩 0.67 GiB 夠不夠、開關各跑一次比對**輸出是否逐字相同**
      （貪婪解碼下應無損，但要驗）。價值在 390 份擴量時
- [ ] retrieval-check.py 的框架對 v2 已過期（docstring 說「19 份未經處理」），
      且頭條數字是假訊號——真正的量是相異雜訊字串 670→13

## v155 遺留（凍結中——資料不修，由重建在對應階段了結）

- W7 的消音＋表格修補**執行**（apply.py 已寫好未套用；520 項消音有 20 份證據）
  → 重建階段 2
- `C Equivalent Networks` 漏詞 15.2%（全庫最大洞；N Flow 8.7%、41598 7.0%）
  → 階段 1 的 coverage 閘門就是為它設的
- 943 處 ∂ 誤讀（909 `\hat{\sigma}` 全在微分位置，掃描器驗過零例外）
  → 階段 2，且「零例外」要在新母體重驗才准機械套用
- `K Muffler` 對照原文可疑率 12.8%（大量「概念 → 引用文獻」型）→ 階段 3 重驗
- 接地檢查 47 個可疑實體（`Region I/II/III`、`Mechl` 等，多與「羅馬數字下標
  難讀」domain_fact 相關）→ 階段 3
- 「qwen 系統性切錯列」缺第二份有空表格的文件當樣本（命中率約 1/15）
- 首頁的期刊/會議資訊（`Paper ID #8776` 型）只在第 0 頁出現一次，現有規則
  抓不到；只有 1 份文件的證據，先不動

## 刻意不做（決策記錄，動它之前先讀理由）

- **實體碎片化的長尾不合併**：51 組被檢索到的裡只有 8 組浪費 ≥2 格（已併），
  另 254 組從未出現在任何檢索結果——254 次不可逆操作換 0 收益。
- **23 處 `\mathsf{P}` 不碰**：P 在語料裡多義（ρ、下標、空間點、矩陣、
  #1056 的壓力 p），沒有便宜可靠的訊號能分開。
- **K Muffler #277 不改**：`eq_similar` 誤報——`\begin{array}` 排版結構被算成
  差異，MinerU 是對的且多帶 `\tag{13}`。
