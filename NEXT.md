# NEXT — 待辦與進行中

規則與契約在 [CLAUDE.md](CLAUDE.md)（SSOT），重建的階段與閘門在
[docs/rebuild-plan.md](docs/rebuild-plan.md)。這裡只放「接下來要做什麼」。
做完就刪——教訓沉澱回文檔，不留屍體；新增時寫清楚證據在哪。

## 進行中：acoustics_v2 重建

階段 0–3 已完成（3 由 Opus 執行 2026-08-03，**待主線獨立驗收**）。
20 份全數索引完成（processed 20/20、failed 0），體檢表 160 格全部填滿：
通過 151、fail 9、驗不了 0、未設定 0。數字見 CLAUDE.md「現況」。

**階段 3 的 fail 9 分兩類，都不是待辦**：3 份是已定案的 waiver
（41598／C 的 `parse.coverage`、N Flow 的 `pp.equations`，note 內已追加
「waiver 2026-08-02（主線）：准進階段 3」）；6 份是 `extract.grounding` >5%
標黃，形狀逐份查過、全部不是幻覺（見下）。

### 階段 3 交出的三件事（材料齊備、等裁決）

- [ ] **`is_symbolic` 的判準要重量**（門檻用量的不要用調的）：
      `SYMBOLIC_RATIO=0.35` 把同一族的東西切成兩半 —— 散文比例低的落進
      「驗不了」、高的落進「可疑」。K Muffler 15.1% 的 92 個可疑裡只有 1 個
      是引用文獻，其餘全是傳遞矩陣的裸符號被命名（`Coefficient Ta`、
      `Matrix GA`、`H12 Parameter`）；G Porous 6.4% 是同一件事的 Bessel 版。
      材料＝那 120 個名字，全在體檢表 note 裡。
- [ ] **`retrieval-check.py` 的框架對 v2 已過期**：它的 docstring 寫「目前索引裡
      19 份是未經處理的，所以雜訊還在，量得到」——v2 全部處理過了，它現在量的是
      「消音字串仍出現在檢索結果裡的比例」。實測 v2 0.57%／55% 的 chunk 命中，
      **但那是偵測器假訊號**：命中集中在單字元的消音字串（`d` 9,059 次、
      `C` 385、`K`／`G`／`N`）與同時是真標題的書眉（`Muffler Acoustics`）。
      真正的量是**相異字串數 670（v155）→ 13（v2）**，消音確實生效。
      要嘛給它一個新問題，要嘛讓它報相異字串數。
- [ ] **實體碎片化在 v2 重量**（rebuild-plan 階段 4「量了才修」）：
      舊庫 388 個多餘節點裡 254 組從未被檢索到，v2 的對應數字還沒量。

### v155 遺留：階段 3 已了結的兩條

- ~~`K Muffler` 可疑率 12.8%（大量「概念→引用文獻」型）~~ → **重驗完成，
  結論被推翻**：v2 15.1%，92 個可疑裡只有 1 個是引用文獻，實際是
  「符號→概念命名」的邊界效應。轉為上面的 `is_symbolic` 待辦。
- ~~接地檢查 47 個可疑實體（`Region I/II/III`、`Mechl`）~~ → **重驗完成**：
  v2 C 的可疑降到 14 個，仍是羅馬數字下標與 `Mechl` 同一族，
  對應 domain_fact「羅馬數字下標難讀」，維持不修。

## 階段 2.7 收尾交出的四件事（2026-08-02，材料齊備、等裁決）

- [ ] **表格的「結構黏連」不在後處理射程**（c-tables-disputes §7.5 定案指定記這裡）：
      `<td rowspan=2>ResistorCapacitorCoil</td>` —— MinerU 把三列標籤塞進一格且
      **沒有分隔符**，內容沒掉、分隔符掉了（檢索 "Resistor" 配不到）。C 的
      table 漏詞裡這一類佔 72%。救它必須重排表格結構＝整表換掉，與「定點補格、
      現值一個字不動」互斥。**建議在解析階段解**（MinerU 選項／版面規則），
      不是後處理議題。同輪已了結的另一半（`\mathsf{t a n h}` 逐字母排版）
      走機械正規化，2,399 段。
- [ ] **`\times` 誤讀的第二族：指數裡的座標 x**（C 6 處，`e^{-\gamma_{n,v}\times}`）。
      病因與已套用的下標族相同（裁圖 t373 上寫的是 `e^{-γ_n x}`），但**位置不同**，
      不在 §7.4 授權的錨點內，本輪一處未碰。規則一次只放寬一條，否則漂移是誰
      造成的分不出來。
- [ ] **N Flow #1410（式 27，p59）的 `\mathbf{\overline{\partial}}`**：同一式裡並排的
      另一個 `\frac{\partial^2}{\partial t^2}` 讀對了。但正解**不是**換 token ——
      那道橫槓多半是罩住整個四階相關量乘積的系綜平均線被掛錯位置（Proudman
      四階相關式的標準寫法），修法是重新界定 overline 的範圍＝整條重轉錄。
      這也是 N Flow `pp.equations` 維持 fail 的唯一理由。
- [ ] **C 的 91 個「bbox 未覆蓋」詞**（歸因見 `records/review/c-uncovered-words.md`）：
      19 個 `continued` 續表標籤、19 個表格標題行、53 個在 p33／p51 兩頁
      （MinerU 整塊發成 image、caption 是 OCR 亂碼）。前兩類位置固定、字面單一，
      機械可修，但**會增加項目數**，與「項目數不得改變」的鐵則衝突
      （sidecar 的 self_ref 是陣列索引），只能併進既有 item 的 caption ——
      往哪裡併要先裁決。第三類比照 p64 `#540` 逐塊人工裁定。
      全修 → 10.5% 降到 8.0%，**仍高於 5%**：C 的 waiver 是人的決定，不是技術問題。

## v155 遺留（凍結中——資料不修，由重建在對應階段了結）

- W7 的消音＋表格修補**執行**（apply.py 已寫好未套用；520 項消音有 20 份證據）
  → 重建階段 2
- `C Equivalent Networks` 漏詞 15.2%（全庫最大洞；N Flow 8.7%、41598 7.0%）
  → 階段 1 的 coverage 閘門就是為它設的
- 943 處 ∂ 誤讀（909 `\hat{\sigma}` 全在微分位置，掃描器驗過零例外）
  → 階段 2，且「零例外」要在新母體重驗才准機械套用
- 「qwen 系統性切錯列」缺第二份有空表格的文件當樣本（命中率約 1/15）
- 首頁的期刊/會議資訊（`Paper ID #8776` 型）只在第 0 頁出現一次，現有規則
  抓不到；只有 1 份文件的證據，先不動

## 刻意不做（決策記錄，動它之前先讀理由）

- **實體碎片化的長尾不合併**：51 組被檢索到的裡只有 8 組浪費 ≥2 格（已併），
  另 254 組從未出現在任何檢索結果——254 次不可逆操作換 0 收益。
- **23 處 `\mathsf{P}` 不碰**：P 在語料裡多義（ρ、下標、空間點、矩陣、
  #1056 的壓力 p），沒有便宜可靠的訊號能分開。
- **K Muffler #277 不改**：`eq_similar` 誤報——`\begin{array}` 排版結構被算成
  差異，MinerU 是對的且多帶 `\tag{13}`。
