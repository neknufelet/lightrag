# NEXT — 待辦與進行中

規則與契約在 [CLAUDE.md](CLAUDE.md)（SSOT），重建的階段與閘門在
[docs/rebuild-plan.md](docs/rebuild-plan.md)。這裡只放「接下來要做什麼」。
做完就刪——教訓沉澱回文檔，不留屍體；新增時寫清楚證據在哪。

## 進行中：acoustics_v2 重建

階段 0、1 已驗收（2026-08-02）。20 份解析完、體檢表就位；**三份被 coverage
閘門攔下不得進抽取**：C 16.1%、N Flow 8.7%、41598 7.0%（驗收紀錄與教訓見
rebuild-plan 階段 1）。下一張工單＝**階段 2：後處理與修補**（發 Opus）：

- [ ] **三份超標文件的洞先分類再修**（judgement-flow 第 4 節）：C 已知是
      表格流失（W5 裁圖＋W6 兩眼轉錄修補）；N Flow 與 41598 的洞**形狀還沒
      挖過**——先用 coverage-check 的漏詞清單定位在哪些頁、哪類區塊，
      分類完才決定修法
- [ ] C 的 p64 mangled×1（README 已知 1/209 案例）一併處置
- [ ] `postprocess.py plan` 對 20 份跑規則（消音 520 項的 v2 對應版本）、
      W7 apply 執行
- [ ] 建立 **v2 的 canary 基準**（`canary --update` 初始化），之後
      daily-check 的 v2 段補上 canary
- [ ] eq-check 三票 + **∂ 機械規則在 v2 母體重掃**（配對括號掃描器；
      零例外才准自動套，一個反例降級逐條看）
- [ ] 體檢表補 `pp.preflight`／`pp.tables`／`pp.equations` 三閘門

## v155 遺留（凍結中——資料不修，由重建在對應階段了結）

- W7 的消音＋表格修補**執行**（apply.py 已寫好未套用；520 項消音有 20 份證據）
  → 重建階段 2
- `C Equivalent Networks` 漏詞 15.2%（全庫最大洞；N Flow 8.7%、41598 7.0%）
  → 階段 1 的 coverage 閘門就是為它設的
- 943 處 ∂ 誤讀（909 `\hat{\sigma}` 全在微分位置，掃描器驗過零例外）
  → 階段 2，且「零例外」要在新母體重驗才准機械套用
- `K Muffler` 對照原文可疑率 12.8%（大量「概念 → 引用文獻」型）→ 階段 3 重驗
- 接地檢查 47 個可疑實體（`Region I/II/III`、`Mechl` 等，多與「羅馬數字下標
  難讀」domain_fact 相關）→ 階段 3
- 「qwen 系統性切錯列」缺第二份有空表格的文件當樣本（命中率約 1/15）
- 首頁的期刊/會議資訊（`Paper ID #8776` 型）只在第 0 頁出現一次，現有規則
  抓不到；只有 1 份文件的證據，先不動

## 刻意不做（決策記錄，動它之前先讀理由）

- **實體碎片化的長尾不合併**：51 組被檢索到的裡只有 8 組浪費 ≥2 格（已併），
  另 254 組從未出現在任何檢索結果——254 次不可逆操作換 0 收益。
- **23 處 `\mathsf{P}` 不碰**：P 在語料裡多義（ρ、下標、空間點、矩陣、
  #1056 的壓力 p），沒有便宜可靠的訊號能分開。
- **K Muffler #277 不改**：`eq_similar` 誤報——`\begin{array}` 排版結構被算成
  差異，MinerU 是對的且多帶 `\tag{13}`。
