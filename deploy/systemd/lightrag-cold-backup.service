[Unit]
Description=lightrag 索引本體冷備份（停服務 → 抄目錄 → 開回來 → restic 上傳）
# 沒有新的抽取成果時腳本會自己跳過、完全不停機，回 0 —— 所以「常常什麼都沒做」是正常的。
# 任何非零都是真的出事（停不掉容器／複製失敗／上傳失敗），腳本會自行經 ntfy 通知；
# 這裡再掛一個獨立備援，防的是腳本本身掛掉、根本沒機會通知那種情況。
# 2026-08-07：OnFailure 的備援通知已移除（ntfy 拆掉）。失敗只留在 journal。

[Service]
Type=oneshot
User=@USER@
WorkingDirectory=@REPO@
ExecStart=@REPO@/scripts/backup-cold.sh
# 上傳走 Google Drive，慢的時候要留餘裕；但也不能無上限，否則掛住不會有人知道。
#
# 2026-08-10 從 3600 調到 7200（PO 裁決）。依據是**當天第一次量到的實際上傳速度**：
# 約 6.6 Mbps（0.8 MB/s），4.3G 要約 87 分鐘 —— 一小時的上限必然撞牆。
#
# ⚠ 在此之前有一次「62 分鐘還沒傳完」，我曾拿它當依據，**那是錯的**：
# 當時 restic 對著一個從來沒被 init 過的倉庫重試，一個位元組都沒落地。
# 真實速度是倉庫建好之後才第一次量到的。
#
# 為什麼不設更大：第一次要傳全量，之後 restic 只傳差異（內容去重），
# 每天大概幾分鐘。上限大只影響第一次；而留著上限是因為**卡住要看得出來** ——
# 一支永遠不結束的備份會讓 timer 停在那裡不再觸發（2026-08-10 實測過那個症狀）。
TimeoutStartSec=7200
