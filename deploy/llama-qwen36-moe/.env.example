# deploy/llama-qwen36-moe/.env 的範本。實際的 `.env` 已被 .gitignore 擋住。
#
# 這裡只有一個鍵，但它值得一個檔：**現役容器把金鑰寫在命令列上**
# （`docker inspect` 與 `ps` 都看得到，本機任何使用者可讀）。同一個坑
# 2026-08-03 已經在 backrest 的 restic 密碼上踩過一次，見 scripts/backup-cold.sh:86。
# 改用環境變數不是潔癖，是把已經發生過的洩漏面關掉。

# llama-server 的 --api-key。**必須與 florian-dker 上 lightrag `.env` 的
# LLM_BINDING_API_KEY 相同**，否則 dker 的抽取會全數 401 —— 而 LightRAG 對
# 401 的表現是「文件處理失敗」，不會有人指著金鑰說話。
# 現行值：從跑著的容器撈（不要重新產一組，換金鑰是兩邊同時改的動作）
#   docker inspect llama-qwen36-moe --format '{{json .Config.Cmd}}' | tr ',' '\n' | grep -A1 api-key
LLAMA_API_KEY=
